# 数据权限架构优化落地方案

> 本文档是对《数据权限重构最终确认方案.md》的后续优化补充，不替代、不修改原方案。
> 原方案仍作为当前已落地数据权限模型的基线；本文用于整理后续可选择实施的优化方向、阶段安排和验收口径。

## 1. 文档定位

当前数据权限重构已经完成主线落地：

- 表策略统一登记。
- TenantLine 只服务 `CURRENT_COMPANY`。
- 工单走领域权限。
- 平台目标公司访问通过 `CompanyDataAccessContext` 分离操作身份与数据目标。
- 旧 `@DataScope` SQL 注入链路已清理。

本文关注的是下一步优化，不是推翻现有架构。

优化目标：

1. 降低长期维护成本。
2. 防止 Java / XML 权限口径漂移。
3. 把“非合作总部 / 默认总部”从 TODO 转为正式业务规则。
4. 提供可分阶段、可选择的落地路径。

## 2. 总体判断

当前架构方向是合适的。

售后业务中，工单、总部配置、关系事实、附件、通知等对象的权限语义不同，不能用单一 `company_id` 拦截器统一表达。现有方案采用：

```text
统一登记
分类执行
复杂领域显式校验
权限失败 fail closed
```

这个方向应继续保留。

后续优化的重点不是增加新的通用拦截器，而是：

- 对复杂领域增加清晰的 Policy 边界。
- 对 SQL 权限表达增加测试和扫描防线。
- 对平台目标公司访问增加工程约束。
- 对总部归属快照规则做正式确认。

## 3. 本轮新增正式业务规则

### 3.1 规则名称

建议命名为：

```text
工单总部归属快照规则
```

核心含义：

`work_order.hq_company_id` 是工单创建时确定的总部归属快照，不随后续合作关系变化动态重算。

### 3.2 适用场景

非合作总部配置读取只适用于：

1. 建单。
2. 故障解析。

不把它扩展为通用的“任意网点可读取任意总部配置”能力。

### 3.3 hq_company_id 落值规则

建单时必须写入 `work_order.hq_company_id`：

```text
能解析出总部 -> 绑定解析出的总部
解析不出总部 -> 绑定系统配置的默认总部
```

落值后作为工单归属快照保存。

后续即使服务关系、合作关系、总部签约关系发生变化，历史工单的 `hq_company_id` 不重算。

### 3.4 转单目标规则

转单目标不强制回到总部合作关系。

已确认口径：

- 一二级关系仍可作为服务网络选择依据。
- 对没有解析到总部、走默认总部的工单，不要求转单目标必须与默认总部存在合作关系。
- 转单目标选择应围绕当前业务流转、上下级服务关系、可服务范围等规则处理。
- 不以“目标服务商是否与工单 `hq_company_id` 建立合作关系”作为硬性前置条件。

这会修正原方案中“转单目标选择时再按合作关系过滤”的表述：合作关系可以参与过滤，但不应作为所有工单转单的强制条件。

### 3.5 总部查询边界

总部查询工单的基础边界为：

```sql
work_order.hq_company_id = 当前总部公司ID
```

该条件应作为总部视角工单可见性的主边界。

含义：

- 总部是否能看到工单，不依赖当前是否仍存在合作关系。
- 总部是否能看到工单，不依赖 `work_order_participant` 中是否存在总部参与记录。
- 如果后续保留总部 `data_scope` 或大区筛选，只能在 `hq_company_id = 当前总部` 的基础上额外收窄，不能扩大。

若按本轮最新口径进一步简化，总部业务查询可以统一只按 `hq_company_id` 作为权限边界；大区只作为页面筛选或统计维度时，应另行明确。

### 3.6 网点动作能力

非合作总部或默认总部归属，不限制当前受理网点完成工单。

网点仍可以完整执行：

- 维修。
- 报价。
- 转单。
- 复检。
- 关闭。

动作是否允许仍按现有工单动作权限模型判断：

```text
可见性
事实关系
基础权限点
工单状态 / 服务方式
```

不得因为工单 `hq_company_id` 对应总部与网点没有合作关系，就阻断网点业务闭环。

### 3.7 历史单可见性

后续建立合作关系后，历史单不改变可见性。

历史单的总部归属、总部可见性、统计归属仍以建单时写入的 `hq_company_id` 为准。

服务网点历史参与可见性仍以工单参与方快照、当前受理方、用户参与事实等现有工单领域规则为准。

### 3.8 建议增加来源快照

建议新增一个可选字段记录总部归属来源：

```text
work_order.hq_resolve_source
```

候选值：

```text
RESOLVED  -> 通过条码 / 产品 / 故障解析出总部
DEFAULT   -> 无法解析总部，使用系统默认总部
MANUAL    -> 未来如允许人工指定总部
```

该字段不是权限判断的必要条件，但有助于排查：

- 为什么某张单归属默认总部。
- 为什么总部能看到某张非合作网点工单。
- 为什么后续建立合作关系后历史单没有变化。

若暂不改表，也至少应在建单日志或流转记录中保留可追溯信息。

## 4. 优化方向一：工单权限服务拆分

### 4.1 当前现状

当前工单权限集中在：

```text
jasic-system/src/main/java/com/jasic/aftersales/system/service/WorkOrderPermissionService.java
```

它当前承担：

- `canView`
- `canExecute`
- `listAvailableActions`
- `getReadonlyReason`
- `resolveRelationTags`
- `buildScopedQuery`

第一轮重构中，先集中权限逻辑是正确的，因为它减少了 Java / XML / 前端各自判断的混乱。

### 4.2 风险

随着后续规则增加，该类会变成权限“大类”：

- 新增动作会继续加 `switch`。
- 新增视角会继续改 `canView`。
- 新增列表口径会继续改 `buildScopedQuery`。
- 展示文案也可能继续和权限判断耦合。

长期风险是：

- 单个类过重。
- 权限判断与展示原因混在一起。
- 列表 SQL 上下文构造和详情权限判断边界不清。

### 4.3 目标结构

建议最终演进为：

```text
WorkOrderPermissionService
  作为门面，保留对外方法

WorkOrderVisibilityPolicy
  只负责 canView

WorkOrderActionPolicy
  只负责 canExecute / availableActions

WorkOrderRelationResolver
  只负责事实关系标签

WorkOrderReadonlyReasonResolver
  只负责只读原因文案

WorkOrderQueryScopeBuilder
  只负责构造 WorkOrderScopedQuery
```

### 4.4 落地方式

不建议一次性大拆。

推荐分三步：

#### 第一步：保留门面

保持 `WorkOrderPermissionService` 对外方法不变：

- `canView`
- `canExecute`
- `listAvailableActions`
- `getReadonlyReason`
- `buildScopedQuery`

现有 Controller / Service 不需要改调用。

#### 第二步：先抽可见性

优先抽：

```text
WorkOrderVisibilityPolicy
```

原因：

- 可见性是所有动作权限的前置条件。
- 列表 / 统计 / 详情一致性也以可见性为基础。
- 本轮“总部归属快照规则”会直接影响 `canView`。

#### 第三步：再抽动作和展示

在可见性稳定后，再抽：

- `WorkOrderActionPolicy`
- `WorkOrderReadonlyReasonResolver`
- `WorkOrderQueryScopeBuilder`

### 4.5 验收标准

- 对外接口行为不变。
- 现有工单权限测试全部通过。
- 新增总部归属快照测试通过。
- `WorkOrderPermissionService` 逐步变成门面类，不再直接承载所有规则。

## 5. 优化方向二：列表 / 统计 / 详情防漂移

### 5.1 当前现状

工单详情主要通过 Java 判断：

```text
WorkOrderPermissionService.canView(...)
```

工单列表和统计主要通过：

```text
WorkOrderScopedQuery
WorkOrderMapper.xml
```

这种设计是现实可行的，因为分页和统计必须在数据库层过滤。

### 5.2 风险

Java 和 XML 是两种表达形式。

如果后续只改 Java，不改 XML，会出现：

- 详情能看，列表看不到。
- 列表能看，详情无权。
- 详情能看，但统计不计入。
- 总部 `hq_company_id` 规则在 Java 和 XML 中不一致。

### 5.3 必须建立一致性测试矩阵

建议建立一组工单权限样本，同一批样本同时验证：

```text
列表是否可见
统计是否计入
详情是否可见
只读原因是否正确
availableActions 是否正确
```

### 5.4 建议覆盖矩阵

| 场景 | 列表 | 统计 | 详情 |
|---|---|---|---|
 总部本总部工单 | 可见 | 计入 | 可见 |
 总部非本总部工单 | 不可见 | 不计入 | 不可见 |
 总部默认总部归属工单 | 默认总部可见 | 默认总部计入 | 默认总部可见 |
 总部后续建立合作关系 | 历史单不变化 | 历史统计不变化 | 历史详情不变化 |
 一级网点本公司工单 | 可见 | 计入 | 可见 |
 一级网点下级二级工单 | 按 `ALL` 口径可见 | 计入 | 可见 |
 二级网点本公司工单 | 可见 | 计入 | 可见 |
 `SELF` 维修员本人单 | 可见 | 计入 | 可见 |
 `SELF` 非本人单 | 不可见 | 不计入 | 不可见 |
 历史参与方 | 历史范围可见 | 历史统计计入 | 详情只读 |
 平台账号 | 默认不可见 | 默认不计入 | 默认不可见 |

### 5.5 本轮总部归属规则下的重点测试

必须补充：

1. 解析出总部的工单，总部按 `hq_company_id` 可见。
2. 未解析总部、绑定默认总部的工单，默认总部按 `hq_company_id` 可见。
3. 非合作网点创建的工单，总部仍按 `hq_company_id` 可见。
4. 后续新增合作关系，不改变历史工单总部可见性。
5. 总部详情可见性不依赖 `work_order_participant` 中的总部参与记录。
6. 总部列表 SQL 不依赖 `p.company_id = 当前总部` 作为可见性必要条件。

### 5.6 验收标准

- 每次修改工单权限，必须运行工单一致性测试。
- Java `canView` 与 XML 列表 / 统计口径必须同测。
- 总部视角必须以 `hq_company_id` 为基础边界。

## 6. 优化方向三：表策略注册增加语义说明

### 6.1 当前现状

当前策略注册在：

```text
jasic-framework/src/main/java/com/jasic/aftersales/framework/datapermission/CompanyDataPolicyRegistry.java
```

所有 `@TableName` 实体已注册到：

```text
CURRENT_COMPANY
HQ_OWNER
DOMAIN_POLICY
RELATION_FACT
GLOBAL
```

### 6.2 风险

表名列表只能说明“归入哪类”，但不能说明“为什么归入这类”。

后续维护人员可能误判：

- 看到 `company_id` 就想改成 `CURRENT_COMPANY`。
- 看到通知表就想直接按接收公司隔离。
- 看到文件绑定表就想开放独立查询。
- 看到组织关系表就误认为是某个公司归属表。

### 6.3 建议补充说明的表

| 表 | 当前策略 | 建议说明 |
|---|---|---|
| `sys_file` | `DOMAIN_POLICY` | 文件本身不代表业务权限，权限跟随业务对象 |
| `sys_file_biz` | `RELATION_FACT` | 文件与业务对象绑定事实，不能作为独立入口 |
| `sys_notify_message` | `DOMAIN_POLICY` | 通知先按接收人 / 接收公司，再回业务对象权限 |
| `first_second_relation` | `RELATION_FACT` | 一级 / 二级组织关系事实，由组织关系流程维护 |
| `hq_first_contract` | `RELATION_FACT` | 总部 / 一级签约事实，不是当前登录公司归属 |
| `work_order_*` 子表 | `DOMAIN_POLICY` | 子表不能绕过工单主权限查询 |
| `machine_barcode` | `HQ_OWNER` | `company_id` 表示归属总部，不是当前登录公司 |
| `fault_repair_config` | `HQ_OWNER` | 后台维护按 owner hq，业务读取按 resolved hq |

### 6.4 两种落地方式

#### 方式 A：轻量注释

直接在注册块上方添加注释。

优点：

- 成本最低。
- 不改变运行逻辑。
- 不影响测试。

缺点：

- 无法结构化查询。

#### 方式 B：结构化注册

引入类似：

```java
register("sys_file_biz", RELATION_FACT, "文件与业务对象绑定事实，不能独立授权");
```

优点：

- 语义说明可被测试或工具读取。

缺点：

- 会改动注册结构。
- 当前阶段收益不一定值得。

### 6.5 推荐

第一阶段先做方式 A。

等表策略进一步增多后，再考虑方式 B。

## 7. 优化方向四：Mapper XML 表引用扫描

### 7.1 当前现状

当前启动校验和单测会扫描所有 `@TableName` 实体，确保每张实体表都注册策略。

### 7.2 剩余风险

如果 Mapper XML 直接查询一张没有实体类的物理表，当前实体扫描发现不了。

例如：

```xml
SELECT ...
FROM some_table_without_entity
```

这类表如果没有注册策略，可能绕开策略登记。

### 7.3 建议做轻量扫描

不建议做完整 SQL 解析器。

建议先做轻量规则：

1. 扫描 `jasic-admin/src/main/resources/mapper/**/*.xml`。
2. 提取明显表名：
   - `from xxx`
   - `join xxx`
   - `update xxx`
   - `insert into xxx`
   - `delete from xxx`
3. 排除：
   - 子查询别名。
   - MyBatis 动态片段变量。
   - 临时表。
   - 外部 CRM 表。
4. 对主库业务表要求：
   - 要么存在于 `CompanyDataPolicyRegistry`。
   - 要么在白名单中注明原因。

### 7.4 白名单要求

白名单不能只写表名，必须写原因：

```text
table_name -> 外部数据源表 / 临时表 / 非业务库表 / SQL 片段误识别
```

### 7.5 验收标准

- 新增 Mapper XML 查询主库表时，如果表未注册策略，测试失败。
- 白名单项必须有说明。
- 误报项收敛到可维护范围内。

### 7.6 实施时机

建议放第二阶段。

原因：

- 初次扫描可能会有误报。
- 需要结合现有 XML 写法慢慢收敛。
- 不宜和业务规则调整混在同一轮。

## 8. 优化方向五：CompanyDataAccessContext 使用治理

### 8.1 当前现状

当前上下文类：

```text
jasic-framework/src/main/java/com/jasic/aftersales/framework/datapermission/CompanyDataAccessContext.java
```

核心能力：

- 保存单次调用目标公司。
- 不改变登录态。
- `runWithTargetCompany` 支持嵌套恢复。
- `finally` 中恢复或清理 ThreadLocal。

实现本身没有明显问题。

### 8.2 真正风险

风险不在类本身，而在使用方式分散：

- Controller 直接设置目标公司。
- Service 自己判断平台账号。
- 不走 `CompanyDataAccessService`。
- 异步任务意外继承或依赖 ThreadLocal。
- 平台目标公司参数校验散落在多个入口。

### 8.3 工程约束

建议明确以下规则：

| 规则 | 说明 |
|---|---|
| Controller 不直接操作 `CompanyDataAccessContext` | 入口只接收参数，不处理 ThreadLocal |
| 目标公司解析统一走 `CompanyDataAccessService` | 平台 / 普通用户边界统一 |
| 平台用户访问公司级数据必须显式传 `targetCompanyId` | 不伪装目标公司 |
| 普通用户传其它公司 `targetCompanyId` 必须拒绝 | 防止跨公司越权 |
| 定时任务 / 同步任务不依赖登录态或该 ThreadLocal | 任务内部显式控制范围 |
| 新增公司级入口必须补测试 | 覆盖缺目标公司、非法目标公司、普通用户越权 |

### 8.4 可增加的检查

#### 轻量代码搜索检查

在测试或 CI 中检查：

- `CompanyDataAccessContext` 直接注入点是否只存在于允许类。
- 是否出现手写 `ThreadLocal` 目标公司上下文。

#### 允许直接使用的类

建议允许：

```text
CompanyDataAccessService
MybatisPlusConfig
CompanyDataAccessContextTest
```

其它业务类如需直接使用，应单独说明原因。

### 8.5 验收标准

- 平台目标公司访问入口统一从 `CompanyDataAccessService` 解析。
- 业务 Controller 不直接 set / clear ThreadLocal。
- 异步任务和同步任务测试覆盖上下文清理。

## 9. 优化方向六：非合作总部 TODO 转正式规则

### 9.1 当前状态

原方案中保留了：

```text
TODO: 非合作总部配置读取属于业务兼容场景。后续需结合转单闭环、总部选择、服务关系规则进一步确认。
```

本轮已明确正式规则，见第 3 章。

### 9.2 需要调整的实现口径

#### 总部详情权限

总部详情 `canView` 应以：

```text
work_order.hq_company_id = 当前总部公司ID
```

作为总部可见性的基础条件。

不应额外要求：

```text
work_order_participant 中存在当前总部参与记录
```

#### 总部列表权限

总部列表 SQL 应以：

```sql
w.hq_company_id = #{query.accessContext.currentCompanyId}
```

作为总部可见性的基础条件。

不应把：

```sql
p.company_id = #{query.accessContext.currentCompanyId}
```

作为总部可见性的必要条件。

参与方可用于：

- 历史参与视角。
- 只读原因。
- 兼容已有参与方展示。

但不应成为总部按 `hq_company_id` 查询工单的前置条件。

#### 默认总部工单

如果建单无法解析总部，使用系统默认总部写入 `hq_company_id`。

则默认总部应能按 `hq_company_id` 查询到这张工单。

#### 后续合作关系变化

后续建立合作关系后：

- 不更新历史工单 `hq_company_id`。
- 不回填历史工单总部归属。
- 不改变历史工单总部可见性。

### 9.3 建议测试

新增或调整测试：

1. 解析总部建单：`hq_company_id = 解析总部`。
2. 无法解析总部建单：`hq_company_id = 默认总部`。
3. 总部详情：只要 `hq_company_id = 当前总部` 即可查看。
4. 总部列表：不依赖总部参与方记录。
5. 默认总部：能看到默认总部归属工单。
6. 非合作网点：仍可维修、报价、关闭。
7. 建立合作关系后：历史单可见性不改变。

### 9.4 是否需要改表

建议分两种方案：

#### 方案 A：不改表

只更新规则、代码判断和测试。

优点：

- 成本低。
- 快速统一权限口径。

缺点：

- 排查默认总部来源时不够直观。

#### 方案 B：增加 `hq_resolve_source`

新增字段：

```sql
ALTER TABLE work_order ADD COLUMN hq_resolve_source varchar(32) DEFAULT NULL COMMENT '总部归属来源';
```

建议值：

```text
RESOLVED
DEFAULT
MANUAL
```

优点：

- 可追溯。
- 便于客服、总部、开发排查。

缺点：

- 需要同步实体、VO、SQL、测试、upgrade 脚本。

### 9.5 推荐

如果只是先统一权限口径，选方案 A。

如果后续默认总部场景会频繁排查，建议选方案 B。

## 10. 分阶段落地建议

### 阶段 0：规则确认

目标：只确认口径，不改代码。

内容：

- 确认第 3 章“工单总部归属快照规则”。
- 确认总部查询是否只按 `hq_company_id`，还是保留 `data_scope` 额外收窄。
- 确认是否新增 `hq_resolve_source`。

产出：

- 本文档确认版。
- 如需，后续再补入正式实施清单。

### 阶段 1：低风险加固

目标：不改变大结构，先把规则守住。

建议实施：

1. 在 `CompanyDataPolicyRegistry` 增加易误判表的语义注释。
2. 增加 `CompanyDataAccessContext` 使用约定。
3. 把非合作总部 TODO 改成正式规则注释。
4. 补总部归属快照相关测试。
5. 调整总部 `canView` 和列表 SQL 中依赖参与方的口径。

验收：

- `mvn test`
- `pnpm typecheck`
- `git diff --check`
- 总部默认总部、非合作总部、历史不重算测试通过。

### 阶段 2：防漂移机制

目标：防止后续 Java / XML 权限口径重新分裂。

建议实施：

1. 建立工单列表 / 统计 / 详情一致性测试矩阵。
2. 增加 Mapper XML 表引用扫描测试。
3. 对白名单表写明原因。

验收：

- 新增 Mapper XML 查询未注册表时测试失败。
- 工单权限改动时，一致性测试能覆盖列表、统计、详情。

### 阶段 3：结构演进

目标：降低工单权限类长期维护成本。

建议实施：

1. 保留 `WorkOrderPermissionService` 作为门面。
2. 抽 `WorkOrderVisibilityPolicy`。
3. 抽 `WorkOrderActionPolicy`。
4. 抽 `WorkOrderReadonlyReasonResolver`。
5. 抽 `WorkOrderQueryScopeBuilder`。

验收：

- 对外调用不变。
- 行为测试不变。
- 工单权限相关类职责更清晰。

## 11. 可选实施包

为了方便选择，可以拆成三个实施包。

### 包 A：总部归属规则收口

适合优先做。

内容：

- 非合作总部 TODO 转正式规则。
- 总部详情 `canView` 按 `hq_company_id`。
- 总部列表 SQL 按 `hq_company_id`。
- 默认总部绑定测试。
- 历史单不随合作关系变化测试。

风险：

- 会改变当前总部列表 / 详情可见性口径。
- 需要仔细核对总部 `data_scope` 是否仍作为额外收窄条件。

### 包 B：安全防漂移

适合包 A 后做。

内容：

- 工单列表 / 统计 / 详情一致性测试矩阵。
- Mapper XML 表引用扫描。
- 表策略注册注释。

风险：

- XML 扫描初期可能有误报。
- 需要维护白名单。

### 包 C：结构拆分

适合后续工单规则继续增加时做。

内容：

- 拆 `WorkOrderPermissionService`。
- 引入独立 Policy / Resolver / Builder。

风险：

- 改动范围相对大。
- 如果没有新增业务压力，收益不如包 A / 包 B 直接。

## 12. 推荐优先级

建议优先级：

1. 包 A：总部归属规则收口。
2. 包 B 中的表策略注释和一致性测试。
3. 包 B 中的 Mapper XML 扫描。
4. 包 C：工单权限服务拆分。

不建议现在做的事：

- 不恢复 `@DataScope` SQL 注入。
- 不把表策略放数据库。
- 不把工单强行塞回 `company_id` 通用隔离。
- 不为了减少代码量把总部归属、服务关系、参与方事实混成一个规则。

## 13. 待确认点

后续真正实施前，还需要确认：

1. 总部 `REGION` 数据范围是否继续作为权限收窄条件，还是仅保留为页面筛选 / 统计维度。
2. 是否新增 `work_order.hq_resolve_source`。
3. 默认总部配置缺失时，是拒绝建单，还是允许建单但标记异常。
4. 默认总部停用时，是否禁止使用默认总部建单。
5. 历史单不重算规则是否需要写入系统说明或操作手册。


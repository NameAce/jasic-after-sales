# 佳士售后系统 - 小程序联调清单

> 本文档用于维护“小程序业务参考”与“当前系统后端 / PC 前端落地”之间的联调映射、已确认口径、待确认事项和后续推进顺序。
> 本文档默认参考 `mp` 目录下的小程序源码，不以小程序 mock 接口或小程序代码现状直接覆盖当前正式系统实现。

---

## 1. 使用说明

### 1.1 状态说明

- `已具备待复核`：当前正式系统已具备对应能力，当前重点是按小程序业务口径复核是否存在方向性偏差
- `待联调收口`：业务方向已定，但字段、入口、展示或边界仍需继续收口
- `已有正式结论`：该事项已有明确正式口径，本轮按该结论执行，不再反复讨论
- `本轮不纳入`：该事项已明确不纳入当前正式范围
- `待单独确认`：该事项仍会影响流程边界，需单独确认后再继续
- `后置观察`：暂不纳入当前批次，后续视业务需要再评估

### 1.2 使用边界

- 本文档中的“小程序”仅作为业务参考基线，不代表本轮实际改造对象。
- 本轮所有实际变更范围仅包括：
  - 后端
  - PC/Web 前端 `jasic-ui`
- 本轮不修改小程序代码。
- 如小程序现状与正式业务基线冲突，优先遵循：
  - 对话中已确认规则
  - `docs/00-实施清单.md`
  - `docs/00-小程序联调约定.md`
  - 本文档中的已确认联调口径

---

## 2. 当前总体判断

- 当前系统主业务方向与小程序表达的售后主流程没有明显偏差。
- 当前需要重点收口的，不是“页面是否长得像小程序”，而是业务主链是否一致、正式范围是否一致、PC 端现有页面是否能承接对应口径。
- 重新梳理后，当前联调主链建议按以下顺序理解：
  - 终端用户报修主链
  - 终端用户查单与评价主链
  - 承修方建维修订单主链
  - 承修方处理主链
  - 收尾主链
  - 转单与总部视图主链

---

## 3. 已确认联调口径

| 日期 | 事项 | 结论 |
|------|------|------|
| 2026-04-06 | 本轮实际改造范围 | 只调整后端和 PC/Web 前端 `jasic-ui`，不修改小程序代码 |
| 2026-04-06 | 小程序使用方式 | 小程序源码作为业务流程与页面结构参考；设计图仅作效果参考 |
| 2026-04-06 | 前端范围 | 本轮“前端”仅指当前仓库内的 PC/Web 前端 `jasic-ui` |
| 2026-04-06 | C/B 共用字段联动规则 | 如 C 端联调调整涉及 B 端共用字段口径、接口语义或页面链路，需同步评估并对齐 |
| 2026-04-06 | C 端登录联调口径 | 本轮只收口后端正式登录合同，不要求落地小程序登录页面改造 |
| 2026-04-06 | 建单客户名称来源 | `customerName` 默认取当前登录客户 `nickname`，为空时回退手机号 |
| 2026-04-06 | 服务网点列表形态 | 未授权定位时走完整列表，仅保留“地址 + 电话”口径，不展示距离与导航 |
| 2026-04-06 | 附近网点定位门槛 | 必须授权定位后才允许进入附近网点选择口径 |
| 2026-04-06 | 寄修字段口径 | 不再保留单文本 `shippingInfo` 作为正式口径，改为寄件人姓名 / 手机号 / 地址拆分字段 |
| 2026-04-07 | 非佳士 / 无码报修范围 | 当前正式结论：纳入后续 C 端报修范围；2026-04-08 起页面入口改为只区分“佳士品牌 / 非佳士品牌”，有无条码由后端判定 |
| 2026-04-06 | 总部 / 网点页面扩展方式 | 当前正式结论：默认优先沿用既有 `jasic-ui` 页面，优先在现有工单列表 / 详情页补字段、补按钮、补只读态；如联调收口确有必要，可先说明原因并确认后最小新增路由 / 页签，不默认按小程序原型整套扩新页面 |
| 2026-04-06 | 评价模型方向 | 从单评分升级为三维评分，纳入当前联调清单正式方向 |
| 2026-04-06 | 承修方建维修订单 | 正式纳入本份联调清单主流程 |
| 2026-04-06 | 承修方建维修订单范围 | 同时收口“代客户填写”和“报修一级 / 报修佳士”两类正式口径 |
| 2026-04-06 | 小程序权限口径 | 小程序侧权限沿用现有 B 端权限标识，不单独扩新权限体系 |
| 2026-04-06 | M3 建单提交门槛 | 承修方有码报修必须先查条码，再允许提交建单 |
| 2026-04-06 | M3 故障口径 | 承修方建维修订单统一按“故障描述选项 + 故障备注”收口，直接复用 C 端正式口径 |
| 2026-04-06 | M3 无配置处理 | 条码查询后如无故障配置，直接拦截，不做降级建单 |
| 2026-04-06 | M3 上游目标公司口径 | 二级报修一级、一级报修佳士均按候选目标公司收口：唯一候选自动带出，多个候选允许手选，无候选直接拦截 |
| 2026-04-06 | M3 上游客户匹配口径 | 上游报修手机号保持必填；按手机号匹配已有客户，匹配到则关联，匹配不到不自动新建 |
| 2026-04-06 | M3 建单留痕口径 | 工单保留建单入口类型字段，详情展示，列表不加列 |
| 2026-04-06 | M4 收口范围 | `M4` 只收口到维修完成，返回方式、复检、关闭继续放在 `M5` |
| 2026-04-06 | M4 状态流转口径 | 承修方处理主链继续沿用当前五个内部状态：`PENDING_ASSIGN -> PENDING_TECH_ACCEPT -> IN_PROGRESS -> COMPLETED -> CLOSED` |
| 2026-04-06 | M4 故障判定口径 | `faultJudge` 固定为“有故障 / 无故障”，不再允许自由文本；报价阶段仅要求该字段必填 |
| 2026-04-06 | M4 维修登记改价口径 | 维修登记只允许修改 `quoteAmount` 和 `quoteDesc`，不允许修改 `faultJudge`；如当前无有效报价，需先完成报价动作 |
| 2026-04-06 | M4 报价历史口径 | 首次报价和维修阶段改价均新增一条 `work_order_quote`，旧记录置为 `is_current_valid=0`，最新有效记录作为当前报价，详情展示完整报价历史，后续“是否有故障”等判断统一取最新有效报价 |
| 2026-04-06 | 三维评价落地范围 | 同步扩展到 PC 工单详情、客户评价展示、通知字段 |
| 2026-04-06 | 终端用户联调收口方式 | 因本轮不改小程序代码，默认按“后端合同 + PC 辅助验证”方式收口 |
| 2026-04-06 | M2 接口与状态口径 | 终端用户查单与评价主链按当前正式后端合同收口，不沿用小程序现状接口命名与统计状态命名 |
| 2026-04-06 | 评价入口开放规则 | 只有“已关闭 + 有故障”工单才能评价 |
| 2026-04-06 | “有故障”判定来源 | 沿用当前后端口径，按最新有效报价的 `faultJudge` 判断，`无故障` 视为不可评价 |
| 2026-04-06 | 无故障关闭单评价状态 | 已关闭但无故障的工单统一记为 `NOT_OPEN`，列表和详情不展示“去评价”，也不允许提交评价 |
| 2026-04-06 | 三维评价维度 | 固定为“服务时效 / 维修质量 / 服务满意度”，各 `1-5` 分，保留标签和评价内容 |
| 2026-04-06 | PC 辅助验证范围 | 仅使用既有 `jasic-ui` 页面收口，列表保留轻量验证，详情作为主验证页 |
| 2026-04-06 | 三维评价展示范围 | 只在工单详情展示三维评分，不扩展到列表 |
| 2026-04-06 | 历史评价数据处理 | 历史单评分数据本轮不做兼容迁移，按新结构落地，历史数据后续统一清理 |
| 2026-04-06 | 文档落地原则 | 联调清单只写业务模块、边界、状态和约束，不提前写过细的实施落地细节；存在疑问时先确认再写入文档 |

---

## 4. 联调主链索引

> 下列分组按业务主链重排，仅用于联调映射和问题归类，不代表既定实施顺序。

| 分组 | 名称 | 说明 |
|------|------|------|
| M0 | 联调基线 | 本轮范围、参考来源、正式边界、全局口径 |
| M1 | 终端用户报修主链 | 登录、定位、网点、佳士有码 / 非佳士 / 无码建单、寄修字段 |
| M2 | 终端用户查单与评价主链 | 状态统计、列表、详情、补录寄件、评价 |
| M3 | 承修方建维修订单主链 | 代客户填写、报修一级 / 报修佳士、与 PC 新增工单口径对齐 |
| M4 | 承修方处理主链 | 待派单、派单、待接单、接单、故障判定、报价、维修登记、维修完成 |
| M5 | 收尾主链 | 返回方式、复检、关闭、评价回看 |
| M6 | 转单与总部视图主链 | 转单、当前 / 历史口径、总部既有页面视图 |

---

## 5. 已识别联调事项

### 5.1 M0 联调基线

| 编号 | 事项 | 状态 | 说明 |
|------|------|------|------|
| M0.1 | 明确本轮实际改造范围 | 已有正式结论 | 已确认本轮只调整后端和 `jasic-ui`，不修改小程序代码 |
| M0.2 | 明确小程序与设计图的参考边界 | 已有正式结论 | 已确认小程序源码优先作为业务参考，设计图仅作为效果参考 |
| M0.3 | 明确文档写法与落地粒度 | 已有正式结论 | 已确认联调清单按实施清单风格维护，不提前写过细的实现细节 |
| M0.4 | 明确登录、客户名称、网点、寄修等全局口径 | 已有正式结论 | 已确认当前轮次若存在疑问，需先确认后写入文档 |
| M0.5 | 非佳士 / 无码报修范围 | 已有正式结论 | 当前正式结论：纳入后续 C 端报修范围；页面入口只区分“佳士品牌 / 非佳士品牌”，有无条码不单独做前端业务入口，本轮不扩到 B 端建单入口 |
| M0.6 | 评价模型方向 | 已有正式结论 | 当前联调正式方向改为三维评分，后续围绕该模型收口 |
| M0.7 | 总部 / 网点页面扩展方式 | 已有正式结论 | 默认优先沿用既有 `jasic-ui` 页面，优先在现有工单列表 / 详情页补字段、补按钮、补只读态；如联调收口确有必要，可先说明原因并确认后最小新增路由 / 页签，不默认按小程序原型整套扩新页面 |
| M0.8 | C/B 共用字段联动同步规则 | 已有正式结论 | 已确认 C 端联调调整如涉及 B 端共用字段口径、接口语义或页面链路，需同步评估并对齐 |

### 5.2 M1 终端用户报修主链

| 编号 | 事项 | 状态 | 说明 |
|------|------|------|------|
| M1.1 | C端登录态与客户身份来源收口 | 已有正式结论 | 已复核正式后端登录链路，当前客户身份来源与本轮联调口径一致 |
| M1.2 | 定位授权门槛收口 | 已有正式结论 | 已确认必须先授权定位，才允许进入附近网点口径 |
| M1.3 | 服务网点完整列表口径收口 | 已有正式结论 | 已完成未授权完整列表合同收口，仅保留地址、电话，不展示距离与导航，也不返回经纬度 |
| M1.4 | 佳士有码条码查询口径收口 | 已有正式结论 | 已复核当前正式系统条码查询与故障选项联动能力，继续按现有正式后端合同收口 |
| M1.5 | 佳士有码建单合同收口 | 已有正式结论 | 已确认并落地 `customerName` 默认取当前登录客户 `nickname`，为空时回退手机号 |
| M1.6 | 寄修字段结构收口 | 已有正式结论 | 已确认并落地寄件人姓名 / 手机号 / 地址拆分字段，`sendExpressNo` 保持首次建单可空、后续补录 |
| M1.7 | 非佳士 / 无码入口范围收口 | 已有正式结论 | 已确认 C 端页面入口只区分“佳士品牌 / 非佳士品牌”；非佳士相关场景纳入后续 C 端报修范围，有无条码由后端自动判定 |
| M1.8 | 非佳士 / 无码建单合同收口 | 已有正式结论 | 已确认非佳士按非识别口径处理、不做条码识别；当前受理方继续由用户选择服务网点；故障固定按“其它故障 + 文字说明”提交 |
| M1.9 | 非佳士 / 无码默认总部参数口径收口 | 已有正式结论 | 已确认 `hq_company_id` 统一取系统参数 `defaultHqCompanyId`；参数缺失时直接拦截提交，不做隐式兜底；所有网点继续可选，但业务上接受统一归默认总部管理 |
| M1.10 | 非佳士 / 无码字段与展示口径收口 | 已有正式结论 | 已确认新增 `brandName`；`brandName`、`productModel` 均非必填；条码为空允许列表 / 详情按空展示；“有无条码”由后端自动派生，作为规则与统计维度保留 |
| M1.11 | 无码过保提示口径 | 已有正式结论 | 已确认无码过保仅作前端提示，不要求后端留痕 |

### 5.3 M2 终端用户查单与评价主链

| 编号 | 事项 | 状态 | 说明 |
|------|------|------|------|
| M2.1 | 我的工单状态统计口径对齐 | 已有正式结论 | 已按当前正式状态统计合同完成收口，验收以正式后端口径为准，不沿用小程序现状统计状态命名 |
| M2.2 | 我的工单列表字段口径对齐 | 已有正式结论 | 已按当前正式列表口径完成收口，列表仅保留轻量验证，不沿用小程序现状接口命名与入口定义 |
| M2.3 | 工单详情展示口径对齐 | 已有正式结论 | 已完成申请信息、维修过程、寄修信息和评价信息展示收口；后续非佳士 / 无码扩展继续沿用既有详情页，补充展示品牌类型、品牌名与机器型号，列表不额外加列 |
| M2.4 | 待接单阶段寄件信息补录口径对齐 | 已有正式结论 | 已确认仅寄修单且仅待接单阶段允许补录寄件信息，其他状态或到店场景不开放补录 |
| M2.5 | 评价入口开放规则收口 | 已有正式结论 | 已确认并落地只有“已关闭 + 有故障”才能评价；已关闭但无故障统一为 `NOT_OPEN`，不展示入口也不允许提交 |
| M2.6 | 三维评价模型与展示口径收口 | 已有正式结论 | 已确认并落地三维评分“服务时效 / 维修质量 / 服务满意度”，同步收口后端、B 端相关能力、PC 工单详情和通知字段 |

### 5.4 M3 承修方建维修订单主链

| 编号 | 事项 | 状态 | 说明 |
|------|------|------|------|
| M3.1 | 承修方代客户填写建维修订单口径对齐 | 已有正式结论 | 已按正式口径收口：有码报修必须先查条码；代客户填写建单后当前受理方为当前公司自己；故障统一按“故障描述选项 + 故障备注”提交 |
| M3.2 | 承修方报修一级 / 报修佳士口径对齐 | 已有正式结论 | 已按正式口径收口：上游报修手机号必填并先匹配已有客户；目标公司唯一候选自动带出、多个候选允许手选、无候选直接拦截；切换目标公司后刷新条码结果与故障配置 |
| M3.3 | 承修方建维修订单与 PC 新增工单口径对齐 | 已有正式结论 | 已在既有 `jasic-ui` 工单页完成收口，不扩新页面；工单详情展示建单入口类型，列表保持轻量不加列 |

### 5.5 M4 承修方处理主链

| 编号 | 事项 | 状态 | 说明 |
|------|------|------|------|
| M4.1 | 待派单 / 派单口径对齐 | 已有正式结论 | 已确认经理/派单员仅在 `PENDING_ASSIGN` 执行派单，派单后流转到 `PENDING_TECH_ACCEPT`，沿用既有 `jasic-ui` 处理视图 |
| M4.2 | 待接单 / 接单口径对齐 | 已有正式结论 | 已确认维修员仅在 `PENDING_TECH_ACCEPT` 执行接单，接单后流转到 `IN_PROGRESS`，不回退原型“提交后直接待接单”口径 |
| M4.3 | 故障判定 / 报价口径对齐 | 已有正式结论 | 已确认 `faultJudge` 固定为“有故障 / 无故障”且为报价唯一必填；首次报价与维修阶段改价均保留完整报价历史，后续判断取最新有效报价 |
| M4.4 | 维修登记 / 维修完成口径对齐 | 已有正式结论 | 已确认维修登记沿用正式最小字段并允许调整 `quoteAmount` / `quoteDesc`，但不允许修改 `faultJudge`；维修完成流转到 `COMPLETED`，返回方式 / 复检 / 关闭继续归 `M5` |

### 5.6 M5 收尾主链

| 编号 | 事项 | 状态 | 说明 |
|------|------|------|------|
| M5.1 | 返回方式口径对齐 | 已有正式结论 | 已确认本轮按“关闭工单”弹窗内合并填写返回方式 / 回寄单号 / 关闭原因收口，不单独提供 `RETURN_METHOD` 按钮；回寄继续校验回寄单号 |
| M5.2 | 复检登记口径对齐 | 已有正式结论 | 已确认复检结果本轮仅保留“通过 / 继续维修”；复检后按结果保持 `COMPLETED` 或回到 `IN_PROGRESS`；如后续需要转单，按“复检后继续维修，再走转单”处理 |
| M5.3 | 关闭工单口径对齐 | 已有正式结论 | 已确认关闭原因继续必填；关闭后评价入口继续按“已关闭 + 有故障”开放；已关闭但无故障统一记为 `NOT_OPEN`，不展示评价入口也不允许提交 |
| M5.4 | 客户评价回看口径对齐 | 已有正式结论 | 已确认评价回看本轮只收口到正式后端合同和 `jasic-ui` 工单详情页；三维评价不扩展到列表；`mp` 参考代码旧字段命名留待真实小程序联调时处理 |

### 5.7 M6 转单与总部视图主链

| 编号 | 事项 | 状态 | 说明 |
|------|------|------|------|
| M6.1 | 转单闭环与当前 / 历史口径对齐 | 已有正式结论 | 已完成转单后当前处理 / 历史只读切换收口；转单后继续回到现有后端状态流转，并补齐关键自动化测试 |
| M6.2 | 总部既有页面口径对齐 | 已有正式结论 | 已完成既有 `jasic-ui` 页面下的总部权限、列表和详情边界收口；本轮不按小程序原型整套扩新页面 |
| M6.3 | 总部 / 网点页面扩展方式 | 已有正式结论 | 默认优先沿用既有 `jasic-ui` 页面，优先在现有工单列表 / 详情页补字段、补按钮、补只读态；如联调收口确有必要，可先说明原因并确认后最小新增路由 / 页签，不默认按小程序原型整套扩新页面 |

## 6. 当前仍需单独确认的问题

- 暂无

---

## 7. 决策记录

### 2026-04-06

- 确认本轮所有实际变更只落在后端和 `jasic-ui`，不修改小程序代码
- 确认小程序源码作为业务流程与页面结构参考，设计图只作为效果参考
- 确认本轮“前端”范围仅指 `jasic-ui`
- 确认如 C 端联调调整涉及 B 端共用字段口径、接口语义或页面链路，需同步评估并对齐
- 确认登录联调本轮只收口后端正式登录合同，不要求落地小程序登录页面改造
- 确认 `customerName` 默认取当前登录客户 `nickname`，为空时回退手机号
- 确认未授权定位时服务网点走完整列表，仅保留“地址 + 电话”口径，不展示距离与导航
- 确认附近网点必须授权定位后才允许进入对应口径
- 确认寄修正式口径不再沿用单文本 `shippingInfo`，改为寄件人姓名 / 手机号 / 地址拆分字段
- 确认未授权定位的服务网点完整列表不提供导航按钮，也不返回经纬度
- 确认寄修场景 `sendExpressNo` 首次建单可空，保持后续补录口径
- 确认非佳士 / 无码报修当前正式结论为“不纳入本轮”
- 确认总部 / 网点页面扩展方式当前正式结论为“默认优先沿用既有 `jasic-ui` 页面，优先在现有工单列表 / 详情页补字段、补按钮、补只读态；如联调收口确有必要，可先说明原因并确认后最小新增路由 / 页签，不默认按小程序原型整套扩新页面”
- 确认评价模型方向从单评分升级为三维评分，并纳入当前联调清单正式方向
- 确认承修方“建维修订单”正式纳入本份联调清单主流程
- 确认承修方建维修订单本轮同时收口“代客户填写”和“报修一级 / 报修佳士”两类正式口径
- 确认三维评价本轮同步扩展到 PC 工单详情、客户评价展示和通知字段
- 确认终端用户联调链因本轮不改小程序代码，默认按“后端合同 + PC 辅助验证”方式收口
- 确认 M2 查单与评价主链不沿用小程序现状接口命名和统计状态命名，统一按当前正式后端合同收口
- 确认只有“已关闭 + 有故障”工单才能评价
- 确认“有故障”判定沿用当前后端口径，按最新有效报价的 `faultJudge` 判断
- 确认已关闭但无故障的工单评价状态统一记为 `NOT_OPEN`，列表和详情不展示“去评价”，也不允许提交评价
- 确认三维评价维度固定为“服务时效 / 维修质量 / 服务满意度”，各 `1-5` 分，并保留标签和评价内容
- 确认 PC 辅助验证范围仅限既有 `jasic-ui` 页面，列表保留轻量验证，详情作为主验证页
- 确认三维评价只在工单详情展示，不扩展到列表
- 确认本轮涉及到的 B 端相关字段、展示和通知口径需同步调整，避免 C/B 共用语义漂移
- 确认历史单评分数据本轮不做兼容迁移，按新结构落地，历史数据后续统一清理
- 确认小程序联调清单按实施清单风格维护，不提前写过细的落地实现细节
- 确认存在问题或待确认点时，先沟通确认，再写入文档
- 确认小程序侧权限沿用现有 B 端权限标识，不单独扩一套新的权限体系
- 确认 M3 的有码报修必须先查条码，再允许提交建单
- 确认 M3 建单按“故障描述选项 + 故障备注”收口，直接复用 C 端正式口径；无故障配置时直接拦截，不能降级建单
- 确认二级报修一级、一级报修佳士都按候选目标公司收口：唯一候选自动带出，多个候选允许手选，无候选直接拦截
- 确认上游报修手机号保持必填；按手机号匹配已有客户，匹配到则关联，匹配不到不自动新建客户
- 确认代客户填写建单后当前受理方为当前公司自己
- 确认工单保留“建单入口类型”字段用于留痕，详情展示，列表不额外加列
- 确认 `M4` 只收口到维修完成，返回方式、复检、关闭继续放在 `M5`
- 确认 `M4` 正式验收面继续按“后端 + `jasic-ui`”处理，`mp/contractor` 仅作业务参考
- 确认承修方处理主链继续沿用当前五个内部状态：`PENDING_ASSIGN -> PENDING_TECH_ACCEPT -> IN_PROGRESS -> COMPLETED -> CLOSED`
- 确认经理/派单员仅在 `PENDING_ASSIGN` 执行派单，维修员仅在 `PENDING_TECH_ACCEPT` 执行接单
- 确认 `faultJudge` 固定为“有故障 / 无故障”，不再允许自由文本，且报价阶段只要求该字段必填
- 确认维修登记只允许调整 `quoteAmount` 和 `quoteDesc`，不允许修改 `faultJudge`
- 确认首次报价和维修阶段改价均新增一条 `work_order_quote`；旧记录置为 `is_current_valid=0`，最新有效记录作为当前报价
- 确认工单详情展示完整报价历史，后续“是否有故障”等判断统一取最新有效报价
- 确认 `M5.1` 本轮按“关闭工单”弹窗内合并填写返回方式 / 回寄单号 / 关闭原因收口，不单独提供 `RETURN_METHOD` 按钮
- 确认 `M5.2` 复检结果本轮仅保留“通过 / 继续维修”；如后续需要转单，按“复检后继续维修，再走转单”处理，不在复检结果中扩第三种状态
- 确认 `M5.3` 关闭后评价入口继续按“已关闭 + 有故障”开放；已关闭但无故障统一记为 `NOT_OPEN`，不展示评价入口也不允许提交
- 确认 `M5.4` 评价回看本轮只收口到正式后端合同和 `jasic-ui` 工单详情页；`mp` 参考代码里的旧字段命名不作为当前兼容目标，留待真实小程序联调时处理
- 确认 `M6` 默认优先沿用既有 `jasic-ui` 工单列表 / 详情页收口；如联调收口确有必要，可先说明原因并确认后最小新增路由 / 页签
- 确认 `M6` 转单后状态暂按当前正式后端状态流转处理，不额外新增小程序式状态定义；转单后继续回到 `PENDING_ASSIGN`
- 确认 `M6` 转单目标继续按严格边界收口：二级只能转关联一级，一级只能转当前工单归属总部，不允许跨规则转单
- 确认 `M6` 总部历史视图只看“本总部归属下、当前不在总部手里”的工单；非总部 `HISTORY` 扩展范围本轮保持现状，不额外收紧
- 确认 `M6` 本轮交付除联调差异修正外，同时补齐转单闭环、关系判定等关键后端自动化测试，并以此作为正式验收组成部分

### 2026-04-07

- 确认非佳士报修、无码报修纳入后续 C 端报修范围，不再按“本轮不纳入”处理；当时原结论为保留两个独立入口，后续已在 2026-04-08 调整为页面入口仅区分品牌
- 确认非佳士报修一律按无码口径处理，不做条码识别
- 确认非佳士 / 无码报修后端合同基本一致，当前受理方继续由用户选择服务网点
- 确认非佳士 / 无码报修的 `hq_company_id` 统一取系统参数 `defaultHqCompanyId`
- 确认 `defaultHqCompanyId` 缺失时直接拦截提交，提示“默认归属总部未配置”，不做隐式兜底
- 确认非佳士 / 无码报修的故障描述固定按“其它故障 + 文字说明”收口，不走条码与故障配置联动
- 确认无码过保仅作为前端提示，不要求后端留痕
- 确认新增 `brandName` 字段，不复用既有字段；`brandName`、`productModel` 均为非必填
- 确认品牌类型与 `createEntryType` 不是同一概念，需拆开建模；详情展示品牌类型，不加额外业务类型字段
- 确认非佳士 / 无码扩展本轮只做 C 端，不扩到 B 端建单入口

### 2026-04-08

- 确认 C 端报修页面入口只区分“佳士品牌 / 非佳士品牌”，不再保留“非佳士报修 / 无码报修”两个前端独立入口
- 确认“有无条码”不作为前端业务类型入口，由后端根据品牌与条码情况自动判定处理分支
- 确认“有无条码”仍需作为后端规则与统计维度保留，可由后端根据 `barcode` 自动派生
- 确认非佳士报修继续不走我方条码识别主链，保持按非识别口径处理

---

## 8. 待验收记录

- 暂无

---

## 9. 最近完成

- 2026-04-06：完成 `M6` 转单与总部视图主链收口并通过验收，确认转单后继续按当前正式后端状态回到 `PENDING_ASSIGN`，当前处理 / 历史只读切换维持现有正式合同；转单目标继续按“二级只能转关联一级、一级只能转归属总部”严格限制；总部历史视图收口为“本总部归属下、当前不在总部手里”的工单；页面默认优先沿用既有 `jasic-ui` 工单列表 / 详情页，并补齐关键后端自动化测试
- 2026-04-06：完成 `M5` 收尾主链收口并通过验收，确认本轮返回方式继续并入“关闭工单”弹窗收口，不单独提供 `RETURN_METHOD` 按钮；复检结果仅保留“通过 / 继续维修”，如需转单按“复检后继续维修，再走转单”处理；关闭后评价入口继续按“已关闭 + 有故障”开放，已关闭但无故障统一记为 `NOT_OPEN`；评价回看只收口到正式后端合同和 `jasic-ui` 工单详情页，并补齐关键回归测试
- 2026-04-06：完成 `M4` 承修方处理主链收口并通过验收，确认本轮正式验收面继续按“后端 + `jasic-ui`”处理；派单/接单继续沿用 `PENDING_ASSIGN -> PENDING_TECH_ACCEPT -> IN_PROGRESS -> COMPLETED -> CLOSED` 五态流转；`faultJudge` 固定为“有故障 / 无故障”且为报价唯一必填；维修登记允许调整 `quoteAmount` / `quoteDesc` 但不允许修改 `faultJudge`；首次报价与维修阶段改价均新增报价历史、旧记录置为 `is_current_valid=0`，工单详情展示完整报价历史；`M4` 只收口到维修完成，返回方式 / 复检 / 关闭继续留在 `M5`
- 2026-04-06：完成 `M3` 承修方建维修订单主链收口并通过验收，确认承修方有码报修必须先查条码；代客户填写、报修一级、报修佳士三类入口统一落在既有 `jasic-ui` 工单页内；上游目标公司按候选自动带出/手选/无候选拦截规则收口；建单统一按“故障描述选项 + 故障备注”提交，无故障配置直接拦截；工单补充建单入口类型留痕并在详情展示
- 2026-04-06：完成 `M1` 终端用户报修主链收口并通过验收，确认 `customerName` 按 `nickname -> phone` 派生、未授权定位仅走完整列表且只保留地址/电话、附近网点与完整列表字段边界拆清、寄修按拆分字段收口且 `sendExpressNo` 保持后补、无码继续按正式范围拦截
- 2026-04-06：按业务主链重构小程序联调清单，补入“承修方建维修订单”主线，调整状态语义，并把非佳士 / 无码、总部新页面、三维评价、终端用户收口方式改写为当前正式方向
- 2026-04-06：完成 `M2` 终端用户查单与评价主链收口并通过验收，确认查单链按当前正式后端合同收口、不沿用小程序现状接口与统计状态命名；评价规则统一为“已关闭 + 有故障”才可评价，已关闭但无故障记为 `NOT_OPEN`；三维评价按“服务时效 / 维修质量 / 服务满意度”落地，并同步调整后端、B 端相关能力、PC 工单详情和通知字段

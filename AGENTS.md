# 项目代理约定

## 通用入口

- 协作流程、架构边界、技术栈约束、实现原则、状态同步、风险沟通和注释规范，优先参考 `docs/00-项目协作约定.md`。
- 本项目不再设置固定默认业务参考文档。每轮任务需要参考哪些实施方案、需求文档、原型文档或验收清单，由用户在当轮明确指定。
- 如果用户没有指定业务文档，先基于当前代码、用户描述和当轮上下文判断，不主动套用旧文档口径。

## 优先级规则

1. 用户在当前对话中明确确认的规则、范围和结论。
2. 当轮明确指定的实施方案、设计文档或验收清单。
3. 当前代码中已经稳定运行的架构边界、接口契约和数据模型。
4. `docs/00-项目协作约定.md` 中的通用协作和技术约定。
5. 旧需求文档、旧原型或历史实施记录。

旧文档只作为理解历史背景的辅助材料，不自动作为当前业务基线。页面原型只作为交互和展示参考，不直接覆盖已确认业务规则。

## 冲突处理

- 如果用户最新口径、当前代码、历史文档或专项方案之间存在冲突，先说明冲突点、影响范围和建议方案，不直接按旧口径落地。
- 如果实现路径会扩大范围、破坏稳定接口、调整权限模型、删除历史数据或引入新技术，先暂停并和用户确认。
- 如果旧文档明显过时，不应继续引用为默认依据；需要回看旧口径时，必须由当轮任务明确指定。

## 实现约束

- 默认优先按当前项目现有结构直接落地，不过度抽象、不过度封装。
- 只有当重复逻辑已经稳定、边界清晰，且抽取后能明显降低维护成本时，才做收敛。
- 默认不引入新框架、新中间件、新存储方案、新任务系统或新的前端技术栈。
- 后端默认沿用 `Controller -> Service -> Mapper` 分层，以及 `Entity / DTO / VO / Query` 对象命名。
- 前端默认沿用 Vue2 + Element UI 现有风格。

## 注释约定

- 注释统一使用简体中文。
- 新增或重构类、接口、方法时，应说明业务职责、边界、关键流程、参数含义、返回值含义和异常场景。
- 方法内部关键步骤要说明业务目的，尤其是状态流转、权限边界、幂等保护、异步任务、重试、死信、历史兼容和异常兜底。
- 方法内部调用其它方法时，如果调用原因或目的不明显，应在调用前说明为什么调用以及期望达成什么业务目的。
- 修改代码时同步更新相关注释；未改动区域不要为了方便阅读随意删除原有注释。

## 售后业务建模约定

- 售后业务表默认不使用 `as_` 前缀，直接使用业务语义命名，例如 `work_order`、`work_order_flow`。
- 涉及公司归属设计时，默认优先使用语义化公司字段，不盲目给所有表强塞泛化 `company_id`。
- 多角色语义表默认使用语义化公司字段，例如：
  - `work_order`：`current_accept_company_id`、`create_company_id`、`hq_company_id`
  - `work_order_flow`：`from_company_id`、`to_company_id`、`operator_company_id`
- 单归属业务表默认尽量保留 `company_id`，便于数据权限、统计和责任归属。
- 原则上：一条记录如果天然只归属于一个公司，优先带 `company_id`；如果一条记录同时表达多个公司角色，优先拆成多个语义化公司字段。


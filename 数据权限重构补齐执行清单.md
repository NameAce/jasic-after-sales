# 数据权限重构补齐执行清单

> 本文档用于承接“数据权限重构最终确认方案”的剩余落地事项。  
> 后续实施、验收与状态同步，统一以本文档和项目根目录《数据权限重构最终确认方案.md》为准。  
> 如遇表归类不清、权限边界不清、目标公司语义不清的入口，必须暂停确认，不得硬归类、不得默认放行。

---

## 1. 文档定位

- 唯一业务口径：`数据权限重构最终确认方案.md`
- 本轮默认不再以 `docs/01-需求确认清单.md` 作为数据权限改造口径
- 本文档作用：
  - 固化本轮待补齐事项
  - 作为后续逐项实施顺序与验收依据
  - 记录已完成项、暂停确认项、验证结果

---

## 2. 前置要求

- [x] 先重新完整阅读《数据权限重构最终确认方案.md》，以该文档作为唯一业务口径
- [x] 只改后端和 Web 前端 `jasic-ui-antd`，不修改任何小程序目录，包括 `mp/aftersale`、`mp/contractor`
- [x] 不做旧接口、旧测试、旧注释兼容；不符合新权限模型的旧代码、旧注释、旧前端调用可以直接删除
- [x] 如果某张表或某个入口无法明确归类或判断权限边界，必须暂停确认，不硬归类、不默认放行
- [x] 所有缺登录态、缺公司上下文、缺 `targetCompanyId`、策略未知、目标公司非法、权限解析异常等情况统一 fail closed，并返回明确提示

---

## 3. 状态说明

- `TODO`：已确认需要补齐，但尚未开始
- `IN_PROGRESS`：已开始实施，但尚未完成验收
- `DONE`：已完成并通过验收
- `BLOCKED`：存在待确认阻塞，暂停推进

---

## 4. 本轮补齐总览

| 编号 | 事项 | 状态 | 说明 |
|------|------|------|------|
| DP-01 | 完整落地 `CompanyDataAccessContext` 使用 | DONE | 已接入公司地址簿与角色菜单写入等 `CURRENT_COMPANY` 数据访问入口；平台目标公司、普通用户越权目标公司按新模型 fail closed |
| DP-02 | 补齐 `CURRENT_COMPANY` 表写入来源 | DONE | 已检查 `company_address`、`sys_role`、`sys_region`、`sys_oper_log` 写入来源；大区 Web 调用改为 `targetCompanyId`，不再向新增 / 更新传旧 `companyId` |
| DP-03 | 补齐 `machine_barcode` 的 `HQ_OWNER` 重构 | DONE | 已按 `ownerHqId` / 后端解析总部范围显式过滤，平台缺目标总部、非法目标总部、总部跨公司均 fail closed |
| DP-04 | 补齐附件 / 文件权限 | DONE | 已要求文件预览、附件列表、绑定、解绑携带业务对象上下文；工单附件先过工单主权限，无法明确写入动作的通用入口 fail closed |
| DP-05 | 补齐通知 / 待办权限 | DONE | 已按接收人、接收公司收口通知列表 / 计数 / 状态变更；已读、按业务已读、完成、失效待办均补充工单业务对象权限校验 |
| DP-06 | 补齐定时任务 / 同步任务权限 | DONE | 已收紧手动触发入口为平台用户 + 对应权限点；任务执行链路清理目标公司上下文；手动日志记录真实触发人，定时日志记录系统任务身份 |
| DP-07 | 收口 `RELATION_FACT` 表独立入口 | DONE | 已确认 `first_second_relation` / `hq_first_contract` 作为组织关系配置业务流程维护的 `RELATION_FACT`；`/org/contract/*` 统一要求目标总部上下文并做服务层边界校验 |
| DP-08 | 清理旧兼容和误导注释 | DONE | 已删除旧 `resolveRelationType` 测试残留、旧动作展示辅助方法、旧前端字段兼容与未使用旧 API 别名；保留 `DataScopeEnum`、角色 `data_scope`、`SysDataScopeRuleService` |
| DP-09 | 补齐 Web 前端 `jasic-ui-antd` | DONE | 已移除工单列表 / 统计前端权限字段，故障配置详情补传 `ownerHqId`，文件 API 保持业务对象上下文，删除工单旧兼容 API |
| DP-10 | 补齐 SQL | DONE | 已同步 `init-data.sql` 大区授权权限点；本次重构 upgrade 脚本补齐通知接收公司索引重建、同步任务触发审计字段与大区授权权限点；`schema.sql` 结构已与新口径一致 |
| DP-11 | 补齐测试和验收 | DONE | 已完善专项测试并执行 `mvn test`、`pnpm typecheck`、`git diff --check` |

---

## 5. 分项实施清单

### DP-01 完整落地 `CompanyDataAccessContext` 使用

**状态：DONE**

- [x] 现有 `CompanyDataAccessContext` 不再只停留在框架层
- [x] 平台维护 `CURRENT_COMPANY` 公司级数据时，显式接收并校验 `targetCompanyId`
- [x] 平台用户不能伪装成目标公司，不能修改 `SecurityContext.currentCompanyId`
- [x] 通过 `CompanyDataAccessContext.runWithTargetCompany(...)` 包裹对应数据访问
- [x] 普通公司用户如果传 `targetCompanyId`，必须等于当前公司，否则拒绝
- [x] 新增 / 更新测试，覆盖缺 `targetCompanyId`、非法 `targetCompanyId`、普通用户越权传 `targetCompanyId`

### DP-02 补齐 `CURRENT_COMPANY` 表写入来源

**状态：DONE**

- [x] 检查所有注册为 `CURRENT_COMPANY` 的表：`company_address`、`sys_role`、`sys_region`、`sys_oper_log`
- [x] 新增数据 `company_id` 必须由后端可信上下文写入，不信任前端
- [x] 重点检查 `sys_region` 是否仍直接使用 DTO.`companyId` 写入，按新模型修正
- [x] 平台维护公司级数据如需要 `targetCompanyId`，按 DP-01 实现
- [x] 裸 `updateById` / `deleteById` 仅在 TenantLine 生效且上下文明确时允许

### DP-03 补齐 `machine_barcode` 的 `HQ_OWNER` 重构

**状态：DONE**

- [x] `machine_barcode` 已注册为 `HQ_OWNER`，按 `HQ_OWNER` 语义改造
- [x] 禁止依赖 TenantLine
- [x] 后台维护按 `ownerHqId` 显式过滤
- [x] 业务读取按 `resolvedHqId` 显式过滤
- [x] 写操作先校验归属总部，再更新 / 删除，或更新条件中显式带 `ownerHqId`
- [x] 平台维护必须指定 `targetCompanyId` / `ownerHqId`，并校验目标公司是总部
- [x] 总部用户只能维护当前总部条码数据
- [x] 新增测试覆盖 HQ 用户、平台缺目标公司、平台目标公司非法、跨总部越权、业务 `resolvedHq` 读取

### DP-04 补齐附件 / 文件权限

**状态：DONE**

- [x] 附件和 `sys_file_biz` 不能作为独立权限入口绕过业务对象
- [x] `/system/file/{fileId}/preview-url`、`/system/file/biz/list`、`/system/file/biz/bind`、`/system/file/biz/unbind` 必须按 `bizType` 挂到对应业务对象权限
- [x] 工单附件必须先通过工单主权限 `WorkOrderPermissionService.canView / canExecute` 再读取、绑定、解绑或生成预览地址
- [x] 不能只凭 `fileId` 生成预览地址
- [x] 对暂时无法明确业务归属的 `bizType`，fail closed
- [x] 同步调整 `jasic-ui-antd` 中通用文件 API 调用，必要时改为业务域接口或补充 `bizType` / `bizId` 上下文
- [x] 新增测试覆盖通过文件接口绕过工单权限应失败

### DP-05 补齐通知 / 待办权限

**状态：DONE**

- [x] 通知 / 待办至少按“接收人 + 接收公司 + 业务对象权限”处理
- [x] 如果当前表结构缺接收公司字段，补充 `schema.sql`、`init-data.sql` 和 upgrade 脚本；本轮已补 `schema.sql`、`upgrade-20260418-sys-notify-phase1.sql`、`upgrade-20260505-data-permission-refactor.sql`，`init-data.sql` 无通知消息静态数据，无需调整
- [x] 工单待办列表按接收人和接收公司过滤
- [x] 用户点击进入工单详情时仍走工单详情权限
- [x] 标记已读、按业务已读、完成待办、失效待办不能绕过接收人 / 接收公司 / 业务对象权限
- [x] 新增测试覆盖通知可见但工单详情无权时仍拒绝业务详情访问

### DP-06 补齐定时任务 / 同步任务权限

**状态：DONE**

- [x] 手动触发同步任务入口只能平台用户操作，且必须有对应权限点
- [x] 任务内部执行不得依赖 `SecurityContext` 当前公司
- [x] 手动触发日志记录真实触发人
- [x] 自动调度记录系统任务身份
- [x] 检查 `/system/sync-task/{id}/execute`、`/system/machine-barcode/full-sync` 等入口
- [x] 新增测试覆盖非平台用户即使有普通权限也不能手动触发同步任务

### DP-07 收口 `RELATION_FACT` 表独立入口

**状态：DONE**

- [x] 检查 `sys_user_company`、`sys_user_role`、`sys_role_menu`、`sys_user_region`、`first_second_relation`、`hq_first_contract`、`sys_file_biz`、`work_order_participant`、`work_order_user_participant` 等 `RELATION_FACT` 表
- [x] `sys_user_company`、`sys_user_role`、`sys_role_menu`、`sys_user_region` 仍由用户 / 角色 / 大区业务流程维护；本轮补齐 `sys_user_region` 显式 `targetCompanyId` 解析与 Web 调用上下文
- [x] `sys_file_biz` 仍通过文件业务对象权限服务校验，不作为裸关系表入口
- [x] `work_order_participant`、`work_order_user_participant` 仍挂在工单领域权限和工单流转服务之后，不新增独立入口
- [x] `jasic-ui-antd` 角色菜单授权补传当前目标公司，避免 `sys_role_menu` 关系写入缺公司上下文
- [x] `first_second_relation`、`hq_first_contract` 已按表语义确认为“组织关系配置业务流程维护的 RELATION_FACT”：保留 `/org/contract/*` 业务入口，但不作为任意裸关系表入口；所有读写、删除、CRM 导入均要求显式 `targetCompanyId`
- [x] `hq_first_contract` 以目标总部为边界，后端校验 `targetCompanyId` 必须是启用总部；传入的 `hqCompanyId` 为空时由目标总部写入，非空且不一致时 fail closed
- [x] `first_second_relation` 以目标总部已启用的总部-一级签约关系为边界，列表按目标总部下一级网点过滤，新增 / 删除 / CRM 导入均校验一级网点属于目标总部
- [x] `jasic-ui-antd` 组织关系页已补齐总部-一级、一级-二级关系接口的 `targetCompanyId`；删除旧的 `PUT /org/contract/first-second` 前端调用和编辑入口

### DP-08 清理旧兼容和误导注释

**状态：DONE**

- [x] 删除 `WorkOrderPermissionService` 中注释掉的旧 `resolveRelationType` 代码
- [x] 清理与本次数据权限模型冲突的旧注释、旧 DTO 字段、旧前端类型
- [x] 不为了旧接口保留兼容层
- [x] 保留 `DataScopeEnum`、角色 `data_scope`、`SysDataScopeRuleService`

### DP-09 补齐 Web 前端 `jasic-ui-antd`

**状态：DONE**

- [x] 前端只做展示控制，最终判权在后端
- [x] 删除或调整仍向工单列表 / 统计传服务端权限字段的类型或调用，例如无效 `companyId` 过滤字段
- [x] 故障配置详情如平台场景需要 `ownerHqId` / `targetCompanyId`，前端按后端新接口传递
- [x] 文件相关调用按新的业务对象权限接口调整
- [x] 删除后端已移除接口对应的 API、类型、页面调用
- [x] 不修改 `mp/aftersale`、`mp/contractor`

### DP-10 补齐 SQL

**状态：DONE**

- [x] 如有表结构、菜单、按钮权限变化，同步维护 `schema.sql`、`init-data.sql`
- [x] 新增或更新本次重构 upgrade 脚本，不写空脚本
- [x] 如果确实没有 SQL 变化，脚本中说明原因即可；但如涉及通知接收公司字段或权限点变化，应有真实 SQL

完成记录：
- `schema.sql` 已包含 `sys_notify_message.receiver_company_id`、`idx_receiver_company_status_time`、包含接收公司的 `idx_biz_receiver`，以及 `sync_task_log.trigger_type` / `trigger_user_id`，本轮无需新增结构变更。
- `init-data.sql` 补齐 `system:region:assign` 大区授权按钮权限，匹配后端 `SysRegionController` 与 Web 用户大区授权入口。
- `upgrade-20260505-data-permission-refactor.sql` 补齐通知 `idx_biz_receiver` 旧索引重建、同步任务触发审计字段、大区授权按钮权限及平台管理员 / 平台类型授权。

### DP-11 补齐测试和验收

**状态：DONE**

- [x] 保留并完善现有表策略注册测试
- [x] 新增 / 重写测试覆盖：
  - [x] TenantLine 只作用 `CURRENT_COMPANY`
  - [x] `CURRENT_COMPANY` 缺公司上下文 fail closed
  - [x] 平台 `targetCompanyId` 缺失 / 非法 fail closed
  - [x] `machine_barcode` `HQ_OWNER` 显式过滤
  - [x] 故障配置 `HQ_OWNER` 显式过滤
  - [x] 工单列表 / 统计 / 详情同一权限口径
  - [x] 工单子表不能绕过主工单权限
  - [x] 附件不能绕过业务对象权限
  - [x] 通知 / 待办点击业务详情仍走业务对象权限
  - [x] 同步任务手动触发仅平台可用
- [x] 执行 `mvn test`
- [x] 执行 `jasic-ui-antd` 的 `pnpm typecheck`
- [x] 执行 `git diff --check`
- [x] 最后明确列出已补齐项、仍需暂停确认项、验证结果

---

## 6. 暂停确认记录

> 本节用于记录后续实施中命中的暂停项。

- 2026-05-06 DP-07：`first_second_relation`、`hq_first_contract` 当前存在 `/org/contract/*` Web 业务入口和 CRM 导入入口；但《数据权限重构最终确认方案.md》对这两张表明确要求落地时按表语义确认是否归入 `RELATION_FACT` 或其它策略。本轮不硬归类、不删除入口，需确认它们是否作为“组织关系配置业务流程”保留，或改由其它业务流程维护。2026-05-06 继续处理暂停项时已按现有表语义和策略注册确认：两表保留为组织关系配置业务流程维护的 `RELATION_FACT`，入口不删除，但必须通过目标总部上下文和服务层边界校验，不再作为任意裸关系表入口。
- 2026-05-06 DP-08：未命中新的表归类、接口归类、`bizType` 或权限边界不清问题，无新增暂停确认项。
- 2026-05-07 DP-09：本轮仅处理 `jasic-ui-antd` 前端适配，未命中新表归类、新接口归类、`bizType` 或权限边界不清问题，无新增暂停确认项。
- 2026-05-07 DP-10：本轮仅处理 SQL 补齐，未命中新表归类、新接口归类、`bizType` 或权限边界不清问题，无新增暂停确认项。
- 2026-05-07 DP-11：本轮仅处理测试与验收收口，未命中新表归类、新接口归类、`bizType` 或权限边界不清问题，无新增暂停确认项。

---

## 7. 验证记录

> 本节用于记录每一轮完成后的验证命令和结果。

- 2026-05-06 DP-01：
  - `mvn -pl jasic-system -am "-Dtest=CompanyDataAccessServiceTest,CompanyAddressServiceImplTest,CompanyDataAccessContextTest" test`：未执行成功，PowerShell 将未加引号的逗号解析为参数列表。
  - `mvn -pl jasic-system -am "-Dtest=CompanyDataAccessServiceTest,CompanyAddressServiceImplTest,CompanyDataAccessContextTest" -DfailIfNoTests=false test`：主源码编译通过，`testCompile` 被既有旧测试阻塞；阻塞项包括 `NotifyEventConsumeServiceImplTest`、`MachineBarcodeServiceImplTest`、`SysRegionServiceImplTest`、`NotifyMessageServiceImplTest` 仍引用旧方法签名。本轮按“只处理 DP-01”未扩散修复这些旧测试。
  - `mvn -pl jasic-system -am -DskipTests compile`：通过。
  - `pnpm typecheck`（目录：`jasic-ui-antd`）：通过。
  - `git diff --check`：通过，仅输出既有 LF/CRLF 换行提示。
- 2026-05-06 DP-02：
  - `mvn -pl jasic-system -am -DskipTests compile`：通过。
  - `pnpm typecheck`（目录：`jasic-ui-antd`）：通过。
  - `mvn -pl jasic-system -am "-Dtest=CompanyDataAccessServiceTest,CompanyAddressServiceImplTest,SysRegionServiceImplTest" -DfailIfNoTests=false test`：未执行到测试用例，`jasic-system` 的 `testCompile` 被非本轮旧测试阻塞；阻塞项包括 `NotifyEventConsumeServiceImplTest` 仍未实现 `countTodo(Long, Long)`、`MachineBarcodeServiceImplTest` 仍引用已移除的 `setHqCompanyId`、`NotifyMessageServiceImplTest` 仍引用旧 `countTodo/markRead` 参数签名。
  - `git diff --check`：通过，仅输出既有 LF/CRLF 换行提示。
- 2026-05-06 DP-03：
  - `mvn -pl jasic-system -Dtest=MachineBarcodeServiceImplTest test`：未执行成功，单独编译 `jasic-system` 时未同时构建依赖模块，导致找不到前序任务新增的 `CompanyDataAccessContext`。
  - `mvn -pl jasic-system -am -Dtest=MachineBarcodeServiceImplTest test`：未执行到目标测试，`jasic-common` 无测试用例触发 Surefire `No tests were executed`。
  - `mvn -pl jasic-system -am -Dtest=MachineBarcodeServiceImplTest -DfailIfNoTests=false test`：主源码编译通过，`jasic-system` 的 `testCompile` 被非本轮通知旧测试阻塞；阻塞项为 `NotifyEventConsumeServiceImplTest` 未实现 `countTodo(Long, Long)`、`NotifyMessageServiceImplTest` 仍调用旧 `countTodo/markRead` 参数签名。
  - `mvn -pl jasic-system -am "-Dtest=MachineBarcodeServiceImplTest,WorkOrderServiceImplTest" -DfailIfNoTests=false test`：确认本轮新增 / 调整的条码与工单测试源码已进入编译；仍被同一批通知旧测试 `testCompile` 阻塞，未执行到测试用例。
  - `mvn -pl jasic-system -am -DskipTests compile`：通过。
  - `pnpm typecheck`（目录：`jasic-ui-antd`）：通过。
  - `git diff --check`：通过，仅输出既有 LF/CRLF 换行提示。
- 2026-05-06 DP-04：
  - `mvn -pl jasic-system -am -DskipTests compile`：通过。
  - `mvn -pl jasic-system -am -Dtest=SysFileBizPermissionServiceTest -DfailIfNoTests=false test`：未执行到目标测试，`jasic-system` 的 `testCompile` 被非本轮通知旧测试阻塞；阻塞项为 `NotifyEventConsumeServiceImplTest` 未实现 `countTodo(Long, Long)`，`NotifyMessageServiceImplTest` 仍调用旧 `countTodo/markRead` 参数签名。
  - `pnpm typecheck`（目录：`jasic-ui-antd`）：通过。
  - `git diff --check`：通过，仅输出既有 LF/CRLF 换行提示。
- 2026-05-06 DP-05：
  - `mvn -pl jasic-system -am -DskipTests compile`：通过。
  - `mvn -pl jasic-system -am "-Dtest=NotifyMessageServiceImplTest,NotifyEventConsumeServiceImplTest,WorkOrderServiceImplTest" -DfailIfNoTests=false test`：通知相关测试已执行通过；同命令中的完整 `WorkOrderServiceImplTest` 被非本轮条码 / 默认总部旧用例阻塞，阻塞项为 `shouldReadCreateBarcodeArchiveWithResolvedHqScope`、`shouldLoadBarcodelessProxyCreateInfoFromDefaultHq`，另有本轮新增接收公司断言已后续修正。
  - `mvn -pl jasic-system -am "-Dtest=NotifyMessageServiceImplTest,NotifyEventConsumeServiceImplTest" -DfailIfNoTests=false test`：通过，13 个用例全部通过。
  - `mvn -pl jasic-system -am "-Dtest=WorkOrderServiceImplTest#shouldMarkTodoReadWhenViewingWorkOrderDetail" -DfailIfNoTests=false test`：通过。
  - `mvn -pl jasic-system -am "-Dtest=WorkOrderServiceImplTest#shouldTechAcceptFaultWorkOrderAndCreateQuote" -DfailIfNoTests=false test`：通过。
  - `git -c safe.directory='E:/桌面/jasic-after-sales' -C 'E:/桌面/jasic-after-sales' diff --check`：通过，仅输出既有 LF/CRLF 换行提示。
- 2026-05-06 DP-06：
  - `mvn -pl jasic-system -am "-Dtest=SyncTaskServiceImplTest,SyncTaskExecutionServiceImplTest" -DfailIfNoTests=false test`：通过，7 个用例全部通过。
  - `pnpm typecheck`（目录：`jasic-ui-antd`）：首次在沙箱内因 `EPERM: operation not permitted, lstat 'C:\Users\HUANGB~1'` 失败；经授权在沙箱外重跑后通过。
  - `git -c safe.directory='E:/桌面/jasic-after-sales' -C 'E:/桌面/jasic-after-sales' diff --check`：通过，仅输出既有 LF/CRLF 换行提示。
- 2026-05-06 DP-07：
  - `mvn -pl jasic-system -am -DskipTests compile`：通过。
  - `mvn -pl jasic-system -am "-Dtest=SysRegionServiceImplTest" -DfailIfNoTests=false test`：通过，6 个用例全部通过。
  - `pnpm typecheck`（目录：`jasic-ui-antd`）：通过。
  - `git -c safe.directory='E:/桌面/jasic-after-sales' diff --check`：通过，仅输出既有 LF/CRLF 换行提示。
  - 继续处理暂停项后，`mvn -pl jasic-system -am "-Dtest=SysContractServiceImplTest" -DfailIfNoTests=false test`：首次因 CRM 导入内部保存未传递 `targetCompanyId` 失败，修正后通过，10 个用例全部通过。
  - 继续处理暂停项后，`mvn -pl jasic-system -am -DskipTests compile`：通过。
  - 继续处理暂停项后，`pnpm typecheck`（目录：`jasic-ui-antd`）：通过。
  - 继续处理暂停项后，`rg -n "updateFirstSecondRelation|/org/contract/first-second.*put|removeHqFirst\([^,]+\)|removeFirstSecond\([^,]+\)|buildCrmFirstSecondImportVOList\([^,]+\)" jasic-system/src/main/java jasic-system/src/test/java jasic-ui-antd/src -g "!*mp/**"`：无匹配，确认旧前端 PUT 调用和旧服务签名已清理。
  - 继续处理暂停项后，`git -c safe.directory='E:/桌面/jasic-after-sales' diff --check`：通过，仅输出既有 LF/CRLF 换行提示。
- 2026-05-06 DP-08：
  - `mvn -pl jasic-system -am "-Dtest=WorkOrderPermissionServiceTest" -DfailIfNoTests=false test`：通过，21 个用例全部通过。
  - `pnpm typecheck`（目录：`jasic-ui-antd`）：通过。
  - `git -c safe.directory='E:/桌面/jasic-after-sales' diff --check`：通过，仅输出既有 LF/CRLF 换行提示。
- 2026-05-07 DP-09：
  - `pnpm typecheck`（目录：`jasic-ui-antd`）：通过。
  - `rg -n "companyId|@deprecated|getWorkOrderCreateOptions|listAssignCandidates|listTransferCandidates|saveWorkOrderMailInfo|WorkOrderMailInfoDTO" jasic-ui-antd/src/service/api/work-order.ts`：无匹配，确认工单列表 / 统计 API 类型不再暴露服务端权限字段，且旧兼容 API 已删除。
  - `rg -n "getFaultRepairConfig\([^,\)]+\)" jasic-ui-antd/src -g "*.ts" -g "*.vue"`：无匹配，确认故障配置详情调用均传入上下文参数位置。
  - `git -c safe.directory='E:/桌面/jasic-after-sales' diff --check -- jasic-ui-antd/src/service/api/work-order.ts jasic-ui-antd/src/service/api/system.ts jasic-ui-antd/src/views/advanced-modules/index.vue jasic-ui-antd/src/views/work-order/index.vue 数据权限重构补齐执行清单.md`：通过。
- 2026-05-07 DP-10：
  - `rg -n "system:region:assign|idx_biz_receiver|receiver_company_id|trigger_type|trigger_user_id" sql/init-data.sql sql/schema.sql sql/upgrade-20260505-data-permission-refactor.sql`：通过，确认基础 SQL 与本次 upgrade 脚本包含本轮需要的权限点、通知接收公司索引口径和同步任务触发审计字段。
  - `git -c safe.directory=E:/桌面/jasic-after-sales diff --check -- sql/init-data.sql sql/schema.sql sql/upgrade-20260505-data-permission-refactor.sql`：通过，仅输出既有 LF/CRLF 换行提示。
  - 本轮仅改 SQL 与执行清单，未执行 `mvn test` / `pnpm typecheck`；原因是没有改 Java / Web 源码，专项验证以 SQL 内容检查和 diff whitespace 检查为准。
- 2026-05-07 DP-11：
  - `mvn test`：首次执行到 `jasic-system` 时失败，阻塞项为 `WorkOrderServiceImplTest.shouldReadCreateBarcodeArchiveWithResolvedHqScope` 与 `shouldLoadBarcodelessProxyCreateInfoFromDefaultHq` 仍沿用旧隐式总部上下文 / wrapper 检查方式。
  - `mvn -pl jasic-system -am "-Dtest=WorkOrderServiceImplTest#shouldLoadBarcodelessProxyCreateInfoFromDefaultHq+shouldReadCreateBarcodeArchiveWithResolvedHqScope" -DfailIfNoTests=false test`：未形成有效单测选择，Surefire 2.12.4 对 `+` 方法选择解析失败。
  - `mvn -pl jasic-system -am "-Dtest=WorkOrderServiceImplTest" -DfailIfNoTests=false test`：修正测试后通过，36 个用例全部通过。
  - `mvn test`：通过，完整 Maven Reactor 全部通过。
  - `pnpm typecheck`（目录：`jasic-ui-antd`）：通过。
  - `git diff --check`：在当前 shell 工作目录识别异常时失败并提示非 Git 仓库；随后使用显式 `-C` 重跑。
  - `git -c safe.directory='E:/桌面/jasic-after-sales' -C 'E:/桌面/jasic-after-sales' diff --check`：通过，仅输出既有 LF/CRLF 换行提示。

# 数据权限重构最终确认方案

> 本文档整理本轮沟通中已经确认的数据权限重构方案。后续开发应以本文为准落地。若实现过程中发现某张表无法明确归类到 `CURRENT_COMPANY` / `HQ_OWNER` / `DOMAIN_POLICY` / `RELATION_FACT` / `GLOBAL`，必须暂停并单独确认，不要硬归类。

## 1. 总体目标

当前系统已经能达到业务结果，但数据权限设计存在多套机制并存、字段语义不统一、部分权限逻辑分散在 Java / XML / 拦截器中的问题。本次重构目标不是改变已确认业务规则，而是把权限模型显式化、统一化、可测试化。

最终方向：

- 工单权限走独立领域权限，不纳入通用 `company_id` 隔离。
- 保留 MyBatis-Plus `TenantLine` 能力，但只服务于显式注册为 `CURRENT_COMPANY` 的表。
- 移除当前 `@DataScope` AOP / ThreadLocal SQL 注入实现，仅保留 `data_scope` 业务概念。
- 所有 `@TableName` 实体表必须注册数据策略，不只包含 `company_id` 的表。
- 表策略统一登记，执行方式按策略分类处理。
- 权限失败统一 fail closed，并返回明确提示。
- 旧接口、旧测试、旧注释不做兼容；开发阶段可以直接清理。
- 本次只改后端与 Web 前端 `jasic-ui-antd`，不修改小程序目录。

## 2. 已确认的核心规则

### 2.1 角色 `data_scope`

角色 `data_scope` 表示“用户在当前公司下的总数据范围”，不是权限点级数据范围。

当同一用户在当前公司下拥有多个角色时：

- 权限点取并集。
- `data_scope` 取最大范围。
- 取到的最大 `data_scope` 对该用户在当前公司下的所有权限点生效。

举例：

- 角色 A：`ALL`，有查看权限。
- 角色 B：`SELF`，有关闭权限。
- 最终用户数据范围为 `ALL`，权限点包含查看与关闭。

这是已接受的设计。后续不做权限点级 `data_scope`，除非有新的明确业务需求。

### 2.2 主体范围定义

平台账号：

- 当前不参与工单业务。
- 不绕过工单权限。
- 不默认查看或操作工单业务数据。
- 当前目的主要是维护基础数据和配置。
- 未来如需查看业务数据，应单独设计“平台审计视图”或通过目标公司方式访问；默认不参与工单操作。

总部账号：

- 总部 `ALL` 表示“本总部归属的全部售后数据”，不是全系统全部数据。
- 工单边界应围绕 `hq_company_id = 当前总部`。
- 总部归属配置表边界应围绕归属总部。

一级服务网点：

- 一级网点 `ALL` 表示“本公司及下级二级网点”。
- 不是全部服务商。

二级服务网点：

- 二级网点可选范围只有 `COMPANY` / `SELF`。
- 二级网点不暴露 `ALL`。
- 二级“本公司全部数据”使用 `COMPANY` 表达。

多公司用户：

- 普通用户即使绑定多个公司，也只看当前选择公司的数据。
- 不做多公司并集。
- 跨公司操作必须切换当前公司上下文。
- 平台账号例外，平台通过显式 `targetCompanyId` 操作公司级数据。

## 3. 表策略模型

### 3.1 策略类型

所有 `@TableName` 实体表必须注册到以下五类之一：

```text
CURRENT_COMPANY
HQ_OWNER
DOMAIN_POLICY
RELATION_FACT
GLOBAL
```

若无法明确归类，必须暂停并向业务方确认。

### 3.2 策略含义

#### CURRENT_COMPANY

含义：

- 表数据天然归属于当前登录公司。
- 可使用 `company_id = 当前公司数据访问 ID` 进行隔离。
- 适合简单公司归属表。

执行方式：

- 由 `TenantLine` 自动拼接公司隔离条件。
- 只对显式注册为 `CURRENT_COMPANY` 的表生效。
- 新增数据的 `company_id` 仍由服务层或填充器明确写入，不依赖 TenantLine 自动补。

典型例子：

- `company_address`
- `sys_role`
- `sys_region`
- `sys_oper_log`（如当前语义确认为公司操作日志）

#### HQ_OWNER

含义：

- `company_id` 表示“归属总部”，不是当前登录公司。
- 总部后台维护时按当前总部隔离。
- 网点 / C 端业务读取时按后端解析出的合法总部读取。

执行方式：

- 默认禁用 TenantLine。
- 服务层显式按 `ownerHqId` 或 `resolvedHqId` 过滤。
- 写操作必须先校验归属总部，再写入或更新。

典型例子：

- `fault_repair_config`

#### DOMAIN_POLICY

含义：

- 表权限无法由单一 `company_id` 表达。
- 必须由领域权限策略统一控制。

执行方式：

- 不走 TenantLine。
- 由领域 Policy 决定读写权限。
- 工单领域由 `WorkOrderAccessPolicy` / `WorkOrderPermissionService` 重构后的等价组件接管。

典型例子：

- `work_order`
- 工单子表可作为 `DOMAIN_POLICY` 辅助表处理，但禁止绕过工单主权限独立对外查询。

#### RELATION_FACT

含义：

- 表中的 `company_id` 只是关系事实的一端，不是数据归属字段。
- 自动按当前公司隔离会误伤业务。

执行方式：

- 不走 TenantLine。
- 不能作为独立权限入口对外开放。
- 由使用该关系事实的服务或领域策略校验。

典型例子：

- `sys_user_company`
- `work_order_participant`
- `work_order_user_participant`
- `first_second_relation` / `hq_first_contract` 是否归入此类或其它类，落地时需按表语义确认。

#### GLOBAL

含义：

- 全局配置或基础字典，不按公司隔离。

执行方式：

- 不走 TenantLine。
- 根据菜单权限、平台权限或具体服务规则控制写操作。

典型例子：

- 字典表
- 菜单表
- 公司类型表
- 系统全局配置表（如果确认为全局）

## 4. 策略注册设计

### 4.1 注册位置

新增 framework 层注册组件：

```text
CompanyDataPolicyRegistry
CompanyDataPolicyType
```

职责：

- 只描述表策略。
- 不放业务逻辑。
- 不做运营配置。
- 不存数据库。
- 使用 Java 静态注册、枚举注册或集中注册方法。

原因：

- 表策略是安全边界，不是可运营配置。
- 放数据库容易被误改。
- 放代码里可审查、可测试、可追溯。

### 4.2 注册范围

所有 `@TableName` 实体必须注册策略，包括没有 `company_id` 的表。

禁止只注册有 `company_id` 的表。原因是没有 `company_id` 不代表没有权限语义。

### 4.3 未注册处理

开发阶段与运行阶段均 fail closed：

- 启动校验发现实体未注册策略，应启动失败。
- 单测扫描发现实体未注册策略，应测试失败。

### 4.4 新增表维护流程

新增实体表时，必须先回答：

1. 这张表是否归属于当前公司？
2. `company_id` 如果存在，表示什么语义？
3. 是否为总部归属？
4. 是否为关系事实？
5. 是否必须由某个领域权限接管？
6. 是否全局共享？

然后选择策略：

```text
CURRENT_COMPANY / HQ_OWNER / DOMAIN_POLICY / RELATION_FACT / GLOBAL
```

如果选择不出来，说明建模或权限边界未想清楚，必须暂停确认。

### 4.5 启动与测试校验

建议同时做两层校验：

- 启动校验：防止测试漏跑。
- 单元测试：提前发现新增表未注册。

测试要求：

- 扫描系统内所有 `@TableName` 实体。
- 每张表必须在 `CompanyDataPolicyRegistry` 中存在策略。
- 表名重复或策略缺失应失败。

## 5. TenantLine 设计

### 5.1 保留 TenantLine

本次保留 MyBatis-Plus `TenantLine` 能力。

但改造为：

- 不再自动扫描所有含 `company_id` 字段的实体。
- 不再由 `CompanyIsolationRegistrar` 根据字段自动判断。
- 只对显式注册为 `CURRENT_COMPANY` 的表生效。

### 5.2 TenantLine 是否忽略表

TenantLine 的 `ignoreTable` 应统一查询 `CompanyDataPolicyRegistry`：

```text
CURRENT_COMPANY -> 不忽略，自动拼 company_id
HQ_OWNER        -> 忽略
DOMAIN_POLICY   -> 忽略
RELATION_FACT   -> 忽略
GLOBAL          -> 忽略
```

### 5.3 缺公司上下文处理

`CURRENT_COMPANY` 表访问时，如果无法解析出公司数据访问 ID：

- 不返回 `NULL`。
- 不返回 `-1`。
- 不静默查空。
- 直接抛权限异常。

提示建议：

- 后台管理端：`缺少公司数据访问上下文` 或 `无权访问该数据`。
- C 端：`数据不存在或无权访问`。

### 5.4 新增数据 company_id 来源

新增数据的 `company_id` 不依赖 TenantLine 自动补。

来源规则：

- 普通公司用户创建 `CURRENT_COMPANY` 数据：后端写当前公司。
- 平台用户创建公司级数据：后端写已校验的 `targetCompanyId`。
- `HQ_OWNER` 数据：后端写已校验的总部公司 ID。
- `DOMAIN_POLICY` 数据：由领域服务计算，例如工单的 `createCompanyId`、`currentAcceptCompanyId`、`hqCompanyId`。

前端传来的公司归属字段不能直接信任。

## 6. CompanyDataAccessContext

### 6.1 背景

平台维护公司级数据时，平台身份不伪装为目标公司。

因此：

- `SecurityContext.currentCompanyId` 不应改成目标公司。
- 权限点和审计仍然保留平台操作者身份。
- 数据隔离需要一个单次调用内的目标公司上下文。

### 6.2 设计

新增 framework 层上下文：

```text
CompanyDataAccessContext
```

职责：

- 保存单次请求 / 单次服务调用内的数据访问目标公司。
- 只用于数据访问隔离。
- 不替代登录态。
- 不写入 session。

TenantLine 取公司 ID 顺序：

```text
CompanyDataAccessContext.targetCompanyId
优先，否则
SecurityContext.currentCompanyId
```

### 6.3 封装执行

禁止到处手写 ThreadLocal set / clear。

提供封装方法，例如：

```java
companyDataAccessContext.runWithTargetCompany(targetCompanyId, () -> {
    // service call
});
```

要求：

- 内部 set。
- `finally` clear。
- 避免 ThreadLocal 泄漏。
- 支持返回值与无返回值调用。

### 6.4 平台访问目标公司数据

平台维护公司级数据时：

- 接口显式传 `targetCompanyId`，或先选择目标公司后由后端解析。
- 必须校验平台用户身份。
- 必须校验平台用户拥有对应操作权限。
- 不修改 `SecurityContext.currentCompanyId`。
- 使用 `CompanyDataAccessContext.runWithTargetCompany(...)` 包裹数据访问。

普通公司用户：

- 不允许传其它公司的 `targetCompanyId`。
- 如果传入且不等于当前公司，应拒绝。

## 7. `@DataScope` 处理

### 7.1 当前结论

保留数据范围概念，移除当前实现。

保留：

- `DataScopeEnum`
- 角色表上的 `data_scope`
- `SysDataScopeRuleService`
- 角色数据范围选项与校验逻辑

移除 / 禁用：

- `@DataScope` 注解实际 SQL 注入能力。
- `DataScopeAspect`
- `DataPermissionInterceptor` 中读取 ThreadLocal SQL 并拼接 where 的逻辑。
- 当前通过 ThreadLocal 拼 SQL 的通用数据权限链路。

### 7.2 原因

售后系统核心数据权限不是通用 `company_id` / `create_by` 能表达的。

特别是：

- 工单没有单一归属公司。
- 故障配置对总部后台和网点业务读取使用不同公司语义。
- 平台账号存在操作者身份与目标公司分离。
- 关系事实表的 `company_id` 不是归属字段。

当前 `@DataScope` 没有真实应用场景，继续保留实现会误导后续开发者。

### 7.3 未来可能

未来如确实需要通用数据权限，不建议复用当前注解拼 SQL 的方式。

建议按模块设计 Policy：

- `WorkOrderAccessPolicy`
- `FaultRepairConfigAccessPolicy`
- `OrgAccessPolicy`
- `LogAccessPolicy`
- `NotifyAccessPolicy`

## 8. 工单权限设计

### 8.1 工单必须走领域权限

工单权限不走 TenantLine。

原因：

- 工单有多个公司语义字段。
- 当前受理公司、建单公司、归属总部、历史参与方、用户参与记录、大区、一级/二级服务网点关系都会影响可见性。
- 单一 `company_id` 无法表达。

### 8.2 工单相关字段语义

工单主表应继续使用语义化公司字段：

- `current_accept_company_id`
- `create_company_id`
- `hq_company_id`

工单流转表继续使用多角色语义字段：

- `from_company_id`
- `to_company_id`
- `operator_company_id`

单归属业务记录可保留 `company_id`，但权限不应绕过工单主权限。

### 8.3 工单辅助表

工单辅助表禁止单独对外查询。

包括但不限于：

- `work_order_participant`
- `work_order_user_participant`
- `work_order_quote`
- `work_order_repair`
- `work_order_fault`
- `work_order_fault_part`
- `work_order_evaluation`
- 工单附件绑定关系

访问方式：

1. 先通过工单主权限判断当前用户是否可查看对应工单。
2. 再读取子表。
3. 子表不能绕过工单主权限作为独立数据入口。

### 8.4 WorkOrderAccessContext

重构时应引入内部访问上下文，避免把服务端权限字段注入到前端查询 DTO。

建议新增：

```text
WorkOrderAccessContext
WorkOrderAccessContextResolver
WorkOrderAccessPolicy
```

`WorkOrderAccessContext` 包含：

- 当前用户 ID
- 当前公司 ID
- 当前主体类型
- 当前公司类型编码
- 有效 `data_scope`
- 当前负责大区 ID 列表
- 关联服务公司范围
- 是否平台账号

`WorkOrderQuery` 应只保留前端筛选字段，不再承载服务端注入的权限上下文字段。

### 8.5 列表与详情同口径

工单列表、统计、详情必须保证同一可见性口径。

禁止出现：

- 列表能看到，点详情提示无权。
- 详情可看，但列表统计不计入。
- XML 和 Java 各自维护一套不一致规则。

落地要求：

- 列表 SQL 与详情 `canView` 使用同一个 `WorkOrderAccessContext`。
- 同一组测试样本同时验证列表可见性与详情可见性。
- 若必须在 SQL 中表达可见性，也应由统一上下文和统一规则派生，不允许前端传权限字段。

### 8.6 动作权限

后端是唯一权威。

工单动作权限应继续按以下顺序判断：

1. 是否能查看该工单。
2. 当前人与工单的事实关系标签。
3. 当前用户是否拥有动作基础权限点。
4. 工单状态、服务方式等实例条件。

前端只根据后端返回：

- `availableActions`
- `readonlyReason`
- 菜单权限

做展示、隐藏和禁用，不复刻最终判权规则。

### 8.7 平台账号与工单

平台账号不默认绕过工单权限。

当前平台没有工单界面，不参与工单业务数据。

未来如需平台查看：

- 单独设计平台审计视图；或
- 通过目标公司方式进入某公司上下文查看。

平台不默认拥有工单操作能力。

## 9. 故障配置 `HQ_OWNER` 设计

### 9.1 表语义

故障配置表的 `company_id` 表示“归属总部”，不是当前登录公司。

总部后台维护时：

- 只能维护本总部配置。
- 过滤条件为 `company_id = 当前总部公司 ID`。

网点 / C 端业务读取时：

- 按服务端解析出的 `hqCompanyId` 读取配置。
- 不使用当前网点公司 ID 过滤。

### 9.2 方法命名建议

为避免语义混乱，建议拆分服务方法：

后台维护类：

```text
xxxForOwnerHq(currentCompanyId)
```

业务解析类：

```text
xxxForResolvedHq(resolvedHqCompanyId)
```

### 9.3 非合作总部业务读取

已确认：业务解析读取不强制校验当前一/二级服务网点与总部存在合作关系。

原因：

- 用户可能拿着焊机直接到网点维修。
- 即使网点没有和某总部建立合作关系，也不能因此完全无法做业务。

当前规则：

- 读取故障配置时可按解析出的 `hqCompanyId` 读取。
- 转单目标选择时再按合作关系、上下级关系、可服务范围过滤。

需保留 TODO：

```text
TODO: 非合作总部配置读取属于业务兼容场景。后续需结合转单闭环、总部选择、服务关系规则进一步确认。
```

## 10. 平台账号与公司级数据

### 10.1 平台维护公司级数据

平台如果维护公司级数据，必须先选择或指定目标公司。

要求：

- 操作者身份仍然是平台用户。
- 目标公司通过 `targetCompanyId` 表达。
- 不伪装成目标公司用户。
- 不修改当前 session 中的 `currentCompanyId` 为目标公司。
- 权限点校验基于平台用户。
- 数据归属过滤基于 `targetCompanyId`。
- 审计日志记录真实平台操作者和目标公司。

### 10.2 公司用户传 targetCompanyId

普通公司用户：

- 默认只能操作当前选择公司数据。
- 如果接口传入 `targetCompanyId`，必须等于当前公司。
- 不等于当前公司时拒绝。

## 11. 写操作权限

写操作和读操作一样受策略约束。

### 11.1 CURRENT_COMPANY

可允许裸 `updateById` / `deleteById`，但仅限注册为 `CURRENT_COMPANY` 的表。

原因：

- TenantLine 会自动补当前公司条件。

但新增时：

- `company_id` 仍由服务层或填充器明确写入。

### 11.2 HQ_OWNER

禁止依赖裸 ID 更新或删除。

要求：

- 先查归属总部。
- 校验当前用户或目标公司是否有权操作。
- 再更新 / 删除。
- 或在更新条件中显式带 `company_id = ownerHqId`。

### 11.3 DOMAIN_POLICY

必须通过领域策略校验。

例如工单：

- 先 `WorkOrderAccessPolicy.canExecute(...)`。
- 再执行工单状态流转或子表写入。

### 11.4 RELATION_FACT

不允许作为独立写入口随意写。

必须由所属业务流程维护，例如：

- 用户公司关系由用户/组织服务维护。
- 工单参与方由工单流转服务维护。

### 11.5 GLOBAL

根据具体菜单权限或平台权限控制。

若属于基础全局配置，通常应由平台账号维护。

## 12. 附件与文件权限

附件 / 文件权限跟随业务对象，不单独按 `sys_file_biz.company_id` 对外开放查询。

工单附件：

- 必须先通过工单主权限。
- 通过 `WorkOrderAccessPolicy.canView(workOrderId)` 后再读取附件。

公司地址 / 配置类附件：

- 跟随对应模块策略。

不允许：

- 通过文件绑定表绕过业务对象权限直接查看附件。

## 13. 通知与待办权限

通知 / 待办按以下层次处理：

1. 接收人。
2. 接收公司。
3. 业务对象权限。

例如工单待办：

- 列表按接收人和接收公司过滤。
- 用户点击进入工单详情时，仍然走工单权限。

通知列表可见不代表业务对象详情一定可操作。

## 14. 定时任务与同步任务

### 14.1 触发入口

定时任务、同步任务的手动触发入口：

- 只能平台用户操作。
- 必须拥有对应操作权限。

### 14.2 任务内部

任务内部执行：

- 不依赖登录态数据权限。
- 不依赖 `SecurityContext` 当前公司。
- 使用任务参数、系统配置或系统身份。
- 显式控制数据范围。

### 14.3 审计身份

手动触发：

- 记录真实触发人。

定时自动执行：

- 记录系统任务身份。

## 15. 权限缓存与重新选择公司

### 15.1 缓存原则

权限、角色、公司关系、公司类型变化后，必须清缓存或要求重新选择公司。

### 15.2 清理粒度

角色菜单变化：

- 清该角色所在公司下相关用户权限缓存。

角色 `data_scope` 变化：

- 清该角色所在公司下相关用户权限缓存。
- 用户下次选择公司时重新计算 `effectiveDataScope`。

用户角色变化：

- 清该用户该公司缓存。

用户公司关系变化：

- 清该用户所有公司缓存。
- 要求重新选择公司。

公司类型 / 主体类型变化：

- 清关联公司用户缓存。
- 风险较高时要求重新登录或重新选择公司。

### 15.3 当前上下文

用户当前公司上下文中的：

- `currentCompanyId`
- `currentSubjectType`
- `currentTypeCode`
- `effectiveDataScope`
- `currentRegionIds`

应在选择公司时初始化或重新计算。

## 16. 无权限响应

### 16.1 后台管理端

后台管理端可以返回明确无权限提示，便于排查。

建议：

- `无权访问该数据`
- `无权查看该工单`
- `缺少目标公司上下文`
- `缺少公司数据访问上下文`
- `无权操作目标公司数据`

### 16.2 C 端客户侧

C 端客户侧更保守。

建议：

- `数据不存在或无权访问`

### 16.3 统一 fail closed

以下情况都应拒绝：

- 缺登录态。
- 缺当前公司。
- 缺平台目标公司。
- 表策略未知。
- 策略解析异常。
- 目标公司不合法。
- TenantLine 无法解析公司数据访问 ID。

禁止默认放行。

## 17. 前端范围

### 17.1 修改范围

本次只改：

```text
jasic-ui-antd
```

不改：

```text
mp/aftersale
mp/contractor
其它小程序目录
```

旧 Web 项目 `jasic-ui` 默认不改，除非后续明确仍在使用。

### 17.2 前端权限职责

前端只做展示，不做最终判权。

前端可以根据：

- 菜单权限。
- 后端返回的 `availableActions`。
- 后端返回的 `readonlyReason`。
- 后端返回的只读状态。

做按钮显示、隐藏、禁用。

前端不得复刻工单最终权限逻辑。

### 17.3 删除旧接口时同步处理

若后端删除旧接口：

- 同步删除 `jasic-ui-antd` 中对应 API 方法。
- 同步删除无用类型。
- 同步删除页面入口或按钮引用。
- 小程序中即使存在类似调用，本次不处理。

## 18. SQL 范围

SQL 可以调整。

建议：

- 同步维护 `schema.sql`。
- 同步维护 `init-data.sql`。
- 新增本次重构 upgrade 脚本，例如：

```text
upgrade-YYYYMMDD-data-permission-refactor.sql
```

若删除旧接口对应菜单或按钮权限：

- 同步清理 SQL。
- 同步清理前端权限引用。

保留仍在使用的权限点，例如：

- `workorder:assign`
- `workorder:repair`
- `workorder:transfer`
- `workorder:review`
- `workorder:close`

实际是否保留以重构后的接口与页面为准。

## 19. 测试策略

旧权限口径测试可以直接删除。

新测试重点覆盖：

1. 所有 `@TableName` 实体均已注册策略。
2. 未注册策略启动失败或测试失败。
3. TenantLine 只对 `CURRENT_COMPANY` 表生效。
4. `CURRENT_COMPANY` 缺公司上下文 fail closed。
5. 平台访问公司级数据缺 `targetCompanyId` fail closed。
6. `HQ_OWNER` 表不走 TenantLine。
7. `HQ_OWNER` 由服务层显式 owner / resolved hq 过滤。
8. 工单列表和详情使用同一可见性口径。
9. 工单子表不能绕过工单主权限对外查询。
10. 附件权限跟随业务对象。
11. 通知 / 待办点击业务详情时仍走业务对象权限。

## 20. 旧接口与旧测试清理

开发阶段不做历史兼容。

原则：

- 历史接口如果不再符合新权限模型，直接删除。
- 前端不再使用的接口，直接删除。
- 权限语义不清的接口，直接删除或重构。
- 对应 DTO / VO / Mapper XML / Service 方法 / Controller 方法 / 测试类同步清理。
- 误导性的旧注释同步删除。

不要为了旧接口保留复杂兼容层。

## 21. 建议实施顺序

### 阶段 1：框架层

1. 新增 `CompanyDataPolicyType`。
2. 新增 `CompanyDataPolicyRegistry`。
3. 注册现有所有 `@TableName` 实体表。
4. 新增启动校验。
5. 新增实体注册扫描测试。
6. 新增 `CompanyDataAccessContext`。
7. 提供 `runWithTargetCompany(...)` 封装。

### 阶段 2：TenantLine 改造

1. 移除自动扫描 `company_id` 的注册机制。
2. TenantLine `ignoreTable` 改为查询 `CompanyDataPolicyRegistry`。
3. 只对 `CURRENT_COMPANY` 表启用 TenantLine。
4. 缺公司上下文直接抛权限异常。
5. 清理不必要的 `@InterceptorIgnore`。
6. 极特殊方法级 ignore 必须保留注释说明原因。

### 阶段 3：移除当前 `@DataScope` SQL 注入链

1. 移除 / 禁用 `DataScopeAspect`。
2. 移除 / 禁用 `DataPermissionInterceptor` 中 ThreadLocal SQL 拼接。
3. 保留 `DataScopeEnum`。
4. 保留角色 `data_scope`。
5. 保留 `SysDataScopeRuleService`。

### 阶段 4：HQ_OWNER 表重构

先从故障配置开始：

1. 将 `fault_repair_config` 明确注册为 `HQ_OWNER`。
2. 后台维护入口按 `ownerHqId` 显式过滤。
3. 业务读取入口按 `resolvedHqId` 显式过滤。
4. 新增非合作总部读取 TODO。
5. 写操作先校验归属，再写。

### 阶段 5：工单权限重构

1. 新增 `WorkOrderAccessContext`。
2. 从 `WorkOrderQuery` 中移除服务端注入权限字段。
3. 列表、统计、详情使用同一访问上下文。
4. 抽象或集中工单可见性规则。
5. 工单子表读取必须挂在工单主权限之后。
6. 调整 XML 或 Mapper 查询，保证不接收前端传来的权限字段。
7. 新增列表 / 详情同口径测试。

### 阶段 6：前端与 SQL 清理

1. 只改 `jasic-ui-antd`。
2. 删除旧接口调用。
3. 删除旧权限引用。
4. 更新菜单 / 按钮权限 SQL。
5. 同步维护 `schema.sql`、`init-data.sql`、upgrade 脚本。

### 阶段 7：测试与回归

1. 跑后端单测。
2. 跑表策略注册测试。
3. 跑工单权限测试。
4. 验证 Web 前端主要权限入口。
5. 不改小程序，不以小程序测试作为本次验收前提。

## 22. 暂停确认规则

开发过程中遇到以下情况必须暂停确认：

1. 某张表无法明确归类到五类策略之一。
2. 某张表的 `company_id` 语义不清。
3. 某接口既像公司级数据，又需要跨公司读取。
4. 某业务读取需要突破已确认边界。
5. 工单列表和详情权限口径出现冲突。
6. 平台目标公司访问无法判断是否应允许。
7. 故障配置读取涉及非合作总部且会影响后续状态流转。
8. 删除旧接口可能影响当前 Web 业务入口。

明确要求：

```text
不要硬归类。
不要用临时 @InterceptorIgnore 绕过去。
不要默认放行。
先暂停，单独确认。
```

## 23. 最终结论

本次重构的核心不是把所有权限塞进一个统一拦截器，而是：

```text
统一登记
分类执行
失败关闭
领域权限独立
服务层显式校验复杂场景
```

最终权限执行模型：

```text
CURRENT_COMPANY -> CompanyDataPolicyRegistry + TenantLine
HQ_OWNER        -> CompanyDataPolicyRegistry + 服务层 owner/resolved hq 显式过滤
DOMAIN_POLICY   -> CompanyDataPolicyRegistry + 领域 Policy
RELATION_FACT   -> CompanyDataPolicyRegistry + 使用方业务校验
GLOBAL          -> CompanyDataPolicyRegistry + 菜单/平台/服务规则
```

后续落地时，应以本文档为准。
